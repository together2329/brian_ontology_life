### Three Important Goal

1. **Firenze ASYNC FIFO Update**: Security Handler Bound 요청 및 정리
2. **MSG Debugging HLD**: Direct 전송 이슈 해결 (하재진 TL)
3. **New ECN / Narrow Burst**: 관련 에러 카운터 및 지원 여부 확인
4. **Admin**: Qtest TBD TC 정리 및 Weekly Report 작성
5. **Flit Error Counter 확장**: 16bit → 32bit 증가 작업

### Happiness Point

> [!TIP] **오늘의 행복 의식 (Ritual)**
> - **아침**: 5시 기상 → 📵 스마트폰 멀리하기 → 📖 행복꾸러미 읽기 (30분)
> - **마음가짐**: "될 수 있을까?"라고 의심하지 말고, **"무조건 된다. 시간의 문제일 뿐이다"**라고 확신하기.
> - **집중**: 머릿속을 비우고, 오직 아래 3가지 목표에만 에너지를 쏟아붓기.

### 🗓️ Plan (계획)

- 05:00 - 06:00 [[기상 및 리추얼]] #에너지100
	- 행복꾸러미 읽기 & 명상 (디지털 디톡스)
- 06:00 - 07:00 [[개인 정비]]
	- 아침 식사 및 출근 준비
- 08:50 - 09:50 [[업무]] #에너지90
	- **Firenze ASYNC FIFO**: Security Handler Update 리스트업
- 10:00 - 11:45 [[업무]] #에너지80
	- **MSG Debugging HLD**: Direct 전송 안되는 이슈 분석
	- **AEAD**: 기능 구현 대신 **검토(Review)** 위주로 가볍게 확인
- 11:45 - 12:45 [[식사]]
	- 건강한 점심, 산책으로 세포 깨우기 🌿
- 13:00 - 15:00 [[업무]] #에너지80
	- **New ECN**: Error Counter 확인 및 Narrow Burst Support 검토
	- **기타 이슈 해결**:
		- **Unlock/Run Timeout**: Unlock 상태에서 Req 나갈 때 Timeout 안되는 현상
		- **ATC**: rdata (0xFFFF_FFFF or 0x0) 구분 가능성 확인
		- **HLD ATS New Mode**: OHC 제외 모드 검토
		- HCT SUB Malformed TLP 디버깅
- 15:00 - 16:00 [[업무]]
	- **Qtest TBD TC 정리**: 40여개 TC 제외 및 정리
- 16:00 - 17:00 [[미팅 or 세미나]]
	- Shared/Dedicated Credit Requirement 구체화
- 17:00 - 18:00 [[회고 및 정리]]
	- **Weekly Report 작성**
	- 오늘 하루 몰입도 체크 및 내일 계획
### Day planner

- 6:40 - 7:10 [[기록]] #에너지90 
	- AI 활용해서 지난주 기반으로 Plan 만들기
	- 아주 유용함, 매일 매일 이렇게 하루를 시작하고, 중간 중간 점검하고, 마무리 해야할 듯
	- 클로드, 제미나이, 코덱스, 퍼플렉시티 모두 활용하자
	- 행복꾸러미, 일잘하는법, 모두 연결하자
- 7:10 - 7:30 [[일상]] #에너지90 
	- 씻고 나갈 준비
- 7:30 - 8:00 [[산책]] #에너지90 
	- 해이 지구 산책 ( 너무 귀여움 )
- 8:00 - 8:40 [[일상]] #에너지80 
	- 출근 길, 버스 기다리는 중
- 8:40 - 9:40 [[경제 공부]] #에너지90 
	- 에너지 관려 기업 공부
	- 에너지 관련 유튜브 링크로 공부
	- 이거 경제 대가들의 분석과 연결하면 좋을 듯 
- 9:40 - 10:00 [[업무]] 
	- 회사 와서 업무 시작 준비 
- 10:00 - 11:45 [[업무]] #에너지80
	- TL, HAL 분석 관련 조언
	- MCTP 검증 플랜 같이 짜기
	- AEAD 검증 정도 검토
- 11:45 - 12:40 [[식사]] #에너지80
	- 건강한 밥상
	- 산책 + 커피 with 성현TL님
- 12:40 - 13:53 [[업무]] #에너지80
	- MSG CLD to HLD 디버깅 (버스에서 Dead Lock 발생한 듯)
	- Weekly Report 작성
		- MCTP, AEAD 진행 상황
		- Async FIFO Cell List Up
		- HLD MSG 디버깅
		- TBD TC Review 
- 13:53 - 14:13 [[업무]] #에너지80
	- MSG 디버깅: bid 내부 unknown 발생 확인
	- ASIC Bus 담당자에게 확인 요청함
- 14:13 - 15:00 [[업무]] #에너지80
	- Security Handler에 DCG 기능 추가 중
	- TDISP Enable 관련 보고
- 15:00 - 15:40 [[미팅 or 세미나]] #에너지70 
	- Weekly Report
- 15:40 - 16:20 [[업무]] #에너지80 
	- security handler dcg 추가 확인
	- bus bid 관련 이슈는 이미 수정됨

### Review

**🌟 행복 꾸러미 & 감사 중간 점검 (Mid-day Review)**

**1. 연결의 행복 (Connection)**
- 성현 TL님과의 점심 식사, 산책, 그리고 커피 한 잔. 동료와 함께 웃으며 걷는 시간 자체가 큰 힐링이자 행복 꾸러미의 실천이었습니다.

**2. 건강한 에너지 (Health)**
- '건강한 밥상'으로 몸을 챙기고, 산책으로 신선한 공기를 마시며 세포를 깨운 점. 나를 소중히 대하는 태도에 감사합니다.

**3. 몰입과 성취 (Achievement)**
- 버스에서 발생한 Dead Lock 이슈의 원인(bid unknown)을 찾아내고, 담당자에게 정확히 확인 요청을 한 것. 문제 해결 과정에서의 몰입과 성취감에 감사합니다.

**4. 성장의 기쁨 (Growth)**
- 오전 경제 공부와 주간 보고 작성을 통해 나만의 인사이트를 정리하고 한 주를 갈무리한 시간. 어제보다 조금 더 성장한 나 자신에게 감사합니다.

---

**📋 하루 종합 리뷰 (End-of-Day Review)**

**✅ 완료한 주요 목표**

| 목표 | 진행도 | 상세 내역 |
|------|--------|----------|
| Firenze ASYNC FIFO Update | 🟢 진행중 | Security Handler에 DCG 기능 추가 완료, TDISP Enable 보고 |
| MSG Debugging HLD | 🟢 해결중 | bid unknown 이슈 발견 → ASIC Bus 담당자 확인 요청 |
| Weekly Report | ✅ 완료 | MCTP/AEAD/FIFO/MSG 디버깅 진행 상황 정리 완료 |
| TBD TC Review | 🟡 부분완료 | Qtest TC 정리 시작 (계획: 40여개) |
| Flit Error Counter | ⏸️ 대기 | 16bit→32bit 작업은 내일 진행 예정 |

**💡 주요 성과 (Achievements)**

1. **문제 해결 능력**
   - MSG Dead Lock 이슈의 근본 원인(bid unknown) 정확히 파악
   - 적절한 담당자(ASIC Bus)에게 에스컬레이션
   - [[두려움에 맞서 용기를 낼 때]] 실천 - 복잡한 디버깅에 도전

2. **주간 정리의 완성도**
   - MCTP, AEAD, Async FIFO, MSG 디버깅 모두 문서화
   - TBD TC 리뷰로 테스트 계획 정비
   - [[데일리리포트 쓰기. 구체화하기]] 실천

3. **기술적 깊이**
   - Security Handler DCG 기능 추가 성공
   - TDISP Enable 관련 보고로 프로젝트 진척
   - [[컴퓨터공학과 전공을 좋아한다]] 재확인

**🎯 행복꾸러미 실천 체크**

| 영역 | 실천 항목 | 횟수 |
|------|----------|------|
| **MIND** | 계획 세우기, 에너지 집중하기, 데일리리포트 | ⭐⭐⭐ |
| **Career** | SoC 개발 즐거움, 가치 인정받기 | ⭐⭐ |
| **Body** | 아침 일찍 일어나기, 산책하기 (해이와 함께) | ⭐⭐ |
| **Relation** | 성현TL님과 소통, 소중한 사람들과 시간 | ⭐ |
| **Finance** | 경제 공부 (에너지 기업 분석) | ⭐ |
| **Fun** | - | - |
| **Multiple** | 커피 마시며 대화 | ⭐ |

**⚡ 에너지 관리**
- 평균 에너지 레벨: **85** (매우 우수)
- 최고 에너지: 아침 6:40-7:10 기록 시간 (90)
- 유지 구간: 10:00-14:44 업무 시간 (80 유지)
- 특징: 아침 리추얼 효과로 하루 종일 높은 에너지 유지

**🔥 오늘의 MVP 순간**
> "AI 활용해서 지난주 기반으로 Plan 만들기 - 아주 유용함"
>
> 클로드, 제미나이, 코덱스, 퍼플렉시티를 모두 활용한 메타인지적 접근. 행복꾸러미와 일잘하는법을 연결하려는 시도가 돋보임. [[온 에너지를 집중하기]]의 실천.

**⚠️ 개선점 (Areas for Growth)**

1. **Fun 영역 부재**
   - 오늘 하루 순수한 여가/휴식 시간 없음
   - 출근길, 업무 시간 모두 경제 공부 또는 업무에 집중
   - 권장: 저녁 30분 [[음악 듣는 것을 좋아한다]] 실천

2. **일부 목표 미완료**
   - Flit Error Counter 작업 미착수
   - TBD TC 정리 부분 완료 (40개 중 일부만)
   - 내일 우선순위 조정 필요

3. **회고 시간 부족**
   - Plan에는 17:00-18:00 회고 및 정리 시간이 있었으나
   - Day planner에 기록 없음 (14:44 이후 기록 중단)
   - 하루 마무리 루틴 강화 필요

**🌱 내일을 위한 인사이트**

1. **기술적 과제**
   - MSG bid unknown 이슈: ASIC Bus 팀 피드백 기다리기
   - Flit Error Counter 16→32bit 작업 착수
   - TBD TC 정리 완료하기

2. **행복꾸러미 균형**
   - Fun 영역 의식적 추가 (저녁 음악 감상, 자연과 함께)
   - [[비오는 날 창문 열고 편안한 의자에 앉아서 맥주한잔]] 고려
   - 과도한 생산성보다 **회복과 충전**

3. **루틴 강화**
   - 아침 리추얼 ✅ (효과 입증)
   - 저녁 회고 루틴 ⚠️ (보강 필요)
   - [[해야할 일들을 우선적으로 먼저 해버릴 때]] 지속

4. **계획의 주도권 (Planning Ownership)**
   - AI가 제안한 계획을 수동적으로 따르기보다, **내 상황에 맞게 재검토하고 조율**하는 과정이 필수
   - "실행 가능한가?"를 스스로 묻고 확정하는 단계 추가하기

**💬 한 줄 소감**
> "AI와 행복꾸러미를 연결하려는 시도. 기술과 성장에 몰입한 하루였지만, 여유와 휴식도 행복의 일부임을 기억하자."

### Thank you

### Happiness Point
```dataviewjs

// 📆 날짜 설정 - 문서 제목에서 추출
let targetDate;

// 현재 문서의 제목에서 날짜 추출 시도 (yyyy-mm-dd 형식 찾기)
const titleDateMatch = dv.current().file.name.match(/(\d{4}-\d{2}-\d{2})/);

if (titleDateMatch) {
  // 제목에서 날짜를 찾은 경우
  targetDate = titleDateMatch[1];
} else if (dv.current().file.day) {
  // 제목에서 날짜를 찾지 못했지만 day 속성이 있는 경우
  targetDate = dv.current().file.day.toISODate();
} else {
  // 둘 다 없는 경우 오늘 날짜 사용
  const today = new Date();
  targetDate = today.toISOString().split('T')[0];
}

// 디버깅용 날짜 표시
dv.paragraph(`🗓️ **분석 날짜:** ${targetDate}`);

// 시간 포맷 함수 (기존 코드와 동일)
function formatDuration(totalMinutes) {
  if (totalMinutes === 0) return "0m";
  const minutes = Math.round(totalMinutes % 60);
  const hours = Math.floor(totalMinutes / 60);
  let result = "";
  if (hours > 0) result += `${hours}h`;
  if (minutes > 0) {
    if (hours > 0) result += " ";
    result += `${minutes}m`;
  }
  return result;
}

// 해당 날짜의 활동 데이터를 저장할 객체
const dailyActivities = {}; // { activityName: duration }
const activityEnergies = {}; // { activityName: { totalEnergy: value, totalDuration: value } }

// 해당 날짜의 파일 가져오기
const filePath = `01_Daily/Daily/${targetDate}.md`;
const page = dv.page(filePath);

if (!page) {
  dv.paragraph(`❌ **${targetDate}** 파일을 찾을 수 없습니다. 파일 경로를 확인하세요: \`${filePath}\``);
} else {
  // 파일 내용 가져오기
  const fileContent = await dv.io.load(page.file.path);

  // --- 섹션 추출 로직 (기존 코드와 동일) ---
  const plannerHeaderText = "Day planner";
  const headerRegex = new RegExp(`^#+\\s+${plannerHeaderText}\\s*$`, 'mi');
  const headerMatch = fileContent.match(headerRegex);
  let sectionContent = "";

  if (headerMatch) {
    const headerStartIndex = headerMatch.index;
    const matchedHeader = headerMatch[0];
    const contentAfterHeaderStart = fileContent.substring(headerStartIndex + matchedHeader.length);
    const nextHeaderMatch = contentAfterHeaderStart.match(/^\s*#{1,}\s+/m);
    const contentEndIndex = nextHeaderMatch ? nextHeaderMatch.index : contentAfterHeaderStart.length;
    sectionContent = contentAfterHeaderStart.substring(0, contentEndIndex).trim();
  }
  // --- 섹션 추출 로직 끝 ---

  if (sectionContent) {
    const lines = sectionContent.split('\n');
    const lineRegex = /^\s*[-*]\s+(?:\*\*)?(\d{1,2}:\d{2})(?:\*\*)?\s*[-–]\s*(?:\*\*)?(\d{1,2}:\d{2})(?:\*\*)?\s*(.*?)(?:\s+#.*)?$/;

    for (const line of lines) {
      if (!line.trim() || line.trim().startsWith('#')) continue;

      const match = line.trim().match(lineRegex);
      if (match) {
        let startTimeStr = match[1];
        let endTimeStr = match[2];
        let activityName = match[3].trim();

        if (startTimeStr.length === 4) startTimeStr = "0" + startTimeStr;
        if (endTimeStr.length === 4) endTimeStr = "0" + endTimeStr;

        // --- 활동 이름 정리 개선 (기존 코드와 유사하게 적용) ---
        const linkMatches = activityName.match(/!?\[\[(.*?)\]\]/g) || [];
        const tagMatches = line.trim().match(/#[\p{L}\p{N}_]+/gu) || []; // 라인 전체에서 태그 검색
        
        // console.log(`Line: "${line.trim()}", 찾은 태그:`, tagMatches); // 디버깅용

        let mainText = activityName;
        for (const link of linkMatches) {
          mainText = mainText.replace(link, '');
        }
        
        let tags = "";
        for (const tag of tagMatches) {
          mainText = mainText.replace(tag, ''); // 본문에서 태그 제거
          tags += tag + " ";
        }

        mainText = mainText.replace(/^\s*[\p{Emoji_Presentation}\p{Emoji}]\s*/u, '');
        mainText = mainText.replace(/^[ *_~]+|[ *_~]+$/g, '');
        mainText = mainText.replace(/[*_~]{1,3}(.*?)[*_~]{1,3}/g, '$1').trim();

        if (linkMatches.length > 0) {
          mainText = mainText + " " + linkMatches.join(" ");
        }

        activityName = (mainText || "(설명 없음)").trim();

        const energyTag = tagMatches.find(tag => tag.startsWith('#에너지'));
        let energyLevel = 0;
        if (energyTag) {
          const energyMatch = energyTag.match(/#에너지(\d+)/u);
          if (energyMatch && energyMatch[1]) {
            energyLevel = parseInt(energyMatch[1]);
            // console.log(`에너지 태그 발견: ${energyTag}, 추출된 레벨: ${energyLevel}`); // 디버깅용
          } else {
            // console.log(`에너지 값 추출 실패: ${energyTag}`); // 디버깅용
          }
        }
        // --- 활동 이름 정리 끝 ---

        try {
          const start = new Date(`${targetDate}T${startTimeStr}:00`);
          const end = new Date(`${targetDate}T${endTimeStr}:00`);

          if (isNaN(start.getTime()) || isNaN(end.getTime())) {
            console.warn(`유효하지 않은 시간 포맷 건너뜀: ${line.trim()}`);
            continue;
          }

          let adjustedEnd = end;
          if (start >= end) {
             if (startTimeStr.startsWith("2") && endTimeStr.startsWith("0")) {
               adjustedEnd = new Date(`${targetDate}T${endTimeStr}:00`);
               adjustedEnd.setDate(adjustedEnd.getDate() + 1);
             } else {
               console.warn(`종료 시간이 시작 시간보다 빠르거나 같은 항목 건너뜀: ${line.trim()}`);
               continue;
             }
          }

          const duration = (adjustedEnd - start) / (1000 * 60); // 분 단위

          if (duration > 0) {
            // 해당 날짜의 활동 정보 저장
            dailyActivities[activityName] = (dailyActivities[activityName] || 0) + duration;

            // 에너지 데이터 저장
            if (energyLevel > 0) {
              if (!activityEnergies[activityName]) {
                activityEnergies[activityName] = { totalEnergy: 0, totalDuration: 0 };
              }
              // 에너지 * 시간 가중치 누적
              activityEnergies[activityName].totalEnergy += energyLevel * duration;
              activityEnergies[activityName].totalDuration += duration;
            }
          }
        } catch (e) {
          console.error(`시간 처리 오류: ${line.trim()}`, e);
          dv.span(`❌ 시간 처리 오류 발생: ${line.trim()}. 개발자 콘솔(Ctrl+Shift+I) 확인`);
        }
      } else if (line.trim()) {
        // 매치되지 않은 라인 로깅 (디버깅용)
        // console.log(`매치되지 않은 라인: "${line.trim()}"`);
      }
    }
  } else {
    dv.paragraph(`ℹ️ ${targetDate} 파일에서 '${plannerHeaderText}' 섹션을 찾을 수 없거나 내용이 없습니다.`);
  }

  // --- 테이블 생성 로직 ---
  const activityNames = Object.keys(dailyActivities);

  if (activityNames.length === 0) {
    dv.paragraph("📊 해당 날짜에 분석할 활동 데이터가 없습니다.");
  } else {
    // 테이블 헤더 생성
    const headers = ["활동", "시간", "비율", "평균 에너지"];

    // 테이블 데이터 생성
    const tableData = [];
    let totalDayMinutes = 0;

    // 해당 날짜의 총 시간 계산
    for (const activity of activityNames) {
      totalDayMinutes += dailyActivities[activity];
    }

    // 각 활동별 데이터 생성
    for (const activity of activityNames) {
      const duration = dailyActivities[activity];
      const formattedDuration = formatDuration(duration);

      // 퍼센트 계산
      const percentage = totalDayMinutes > 0 ? Math.round((duration / totalDayMinutes) * 100) : 0;

      // 평균 에너지 계산
      let avgEnergy = "-";
      if (activityEnergies[activity] && activityEnergies[activity].totalDuration > 0) {
        // 해당 활동의 총 에너지 가중 합계를 총 시간으로 나누어 평균 계산
        const energyAvg = Math.round(activityEnergies[activity].totalEnergy / activityEnergies[activity].totalDuration);
        avgEnergy = `${energyAvg}`;
      }

      tableData.push([
        activity,
        formattedDuration,
        `${percentage}%`,
        avgEnergy
      ]);
    }

    // 테이블 데이터 정렬 (시간 기준 내림차순)
    tableData.sort((a, b) => {
      const getMinutes = (timeStr) => {
        if (timeStr === "-" || timeStr === "0m") return 0;
        let totalMinutes = 0;
        const hourMatch = timeStr.match(/(\d+)h/);
        const minuteMatch = timeStr.match(/(\d+)m/);
        if (hourMatch) totalMinutes += parseInt(hourMatch[1]) * 60;
        if (minuteMatch) totalMinutes += parseInt(minuteMatch[1]);
        return totalMinutes;
      };
      // 시간은 두 번째 열(index 1)
      return getMinutes(b[1]) - getMinutes(a[1]);
    });

    // 테이블 Markdown 생성
    let table = `| ${headers.join(" | ")} |\n`;
    table += `| ${headers.map(_ => "---").join(" | ")} |\n`;
    for (const row of tableData) {
      table += `| ${row.join(" | ")} |\n`;
    }

    // 테이블 출력
    dv.paragraph(table);

    // --- 요약 정보 계산 및 출력 ---
    let totalEnergyWeighted = 0;
    let totalEnergyDuration = 0;

    // 해당 날짜의 전체 평균 에너지 계산
    for (const activity in activityEnergies) {
        totalEnergyWeighted += activityEnergies[activity].totalEnergy;
        totalEnergyDuration += activityEnergies[activity].totalDuration;
    }

    const overallAvgEnergy = totalEnergyDuration > 0 ?
      Math.round(totalEnergyWeighted / totalEnergyDuration) : 0;

    dv.paragraph(`**총 기록 시간:** ${formatDuration(totalDayMinutes)} | **평균 에너지:** ${overallAvgEnergy}`);
  }
}
```