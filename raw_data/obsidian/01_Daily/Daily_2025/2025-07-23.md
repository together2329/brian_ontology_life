### Three Important Goal

- [[Link1 SRIOV에서 FLR 시 AXI Hang 발생]]
- [[Xprop test fail]]
- [[DRS Msg Broken Issue]]
	- 다시 처음부터 디버깅
		- pass 와 fail 비교
		- 어디에 link down 필요한지 다시 살펴보기 
		- counter, data 등
- [[Custom TC PF, VF Register Access Debugging]]
	- PF0만 남기고 다시 실행해보기
- [[Custom TC - Req Error Debugging]]
	- issue가 아직 남아 있음
- Custom TC Progress Update
- [PCIe 6.4 지원 여부 검토]]
- application vf 관련 답변 ( )
- debug ( pipe reg ) 관련 답변 ( csr update 후 )
- [[AION TOP SDC 작성]]
### Day planner

- 7:20 - 7:40 [[일상]] #에너지80 
	- 푹 자고 일어나니 컨디션이 상당히 좋음
	- 이것도 신경 쓰자
	- 씻고 여유롭게 준비
- 7:40 - 8:00 [[산책]] #에너지80 
	- 해이 지구 미야 산책
- 8:00 - 8:50 [[일상]] #에너지80 
	- 요거트 + 여유
	- 커피 한잔
	- 백미러 수리 문의 ( 오늘 도착 )
- 8:50 - 9:40 [[공부]] #에너지80 
	- 소수몽키 유튜브
		- 주식 열풍 ( 100배 이익 ) 
			- https://youtu.be/ge-99OZg1Yc?si=qNObyrMM7_LKnsNh
		- 평범한 개미의 투자 성공 비결
			- https://youtu.be/TUyV6nzzcD0?si=tNHWlWyBY4xmnGo9
- 9:40 - 10:30 [[DRS Msg Broken Issue]] #에너지70 
	- stored data 관련 link down 초기화 , 그런데 ready도 초기화 하니 flush가 안 된다
	- ready는 제외하고 초기화 진행 후 다시 테스트
- 10:30 - 11:00 [[Link1 SRIOV에서 FLR 시 AXI Hang 발생]] #에너지70 
	- FLR 관련해서 왜 AXI hang이 발생했는지 분석하기
		- 우선 SRIOV + Pending TLP 에 FLR Status 할당 모두 부여하고 Simulation 진행
- 11:00 - 11:20 [[Custom TC PF, VF Register Access Debugging]] #에너지70 
	- PF, VF 관련 error 처리 진행
	- 일단 4000개 에러 모두 waive 후 진행
	- 이거 잘 되면 pf, vf 풀고 register 단위로 waive 진행 -> 관련해서 올리기
		- cross check ( 필요한지만 검토 )
- 11:20 - 12:10 [[업무]] #에너지70
	- firenze , grizzly csr 작업
	- firenze ide 업무 논의
	- email 읽기
	- custom tc regression status 확인 ( 좋음 )
- 12:20 - 12:50 [[휴식 & 쉼호흡]] #에너지80 
	- 카페에서 음악도 듣고, claude code로 주식 서치도 하고 휴식
	- 에너지 올라감 !!
- 12:50 - 13:10 [[식사]] #에너지70 
	- 제육 볶음 ! 
	- 맛있게 잘 먹음
- 13:10 - 13:50 [[Link1 SRIOV에서 FLR 시 AXI Hang 발생]] #에너지30 
	- SRIOV 잘 안되서 AXI Hang 여전히 발생 
	- 스트레스 지수 올라감
	- 왜 안되는지 제대로 파악 필요
- 13:50 - 16:00 [[Link1 SRIOV에서 FLR 시 AXI Hang 발생]] #에너지50
	- 제대로 분석하자
	- 놀이로 음악 들으며 즐기기 ( 몰입 ! )
	- 아직 스트레스.. ㅎㅎ
	- 우선... 
		- req_error 없이는 잘 되는지 살펴보는 것도 중요하겠다
	- 추가로... 하나 하나 모듈을 심층 분석하자... ( 제대로 이해하자 )
	- 방법 찾음
		- 이 일을 완전히 나랑 분리하기
		- 나랑 상관없는 일처럼 취급
		- Good !!!
- 16:00 - 18:00 [[DRS Msg Broken Issue]] #에너지50
	- 그냥 다 linkdown 넣어 버리자... ㅋㅋㅋㅋ
	- 일을 완전히 나랑 분리하기
	- 멀리서 온 외계인 처럼 살기
- 18:00 - 18:30 [[식사]] #에너지50 
	- 햄버거
- 18:30 - 19:30 [[DRS Msg Broken Issue]] #에너지40
	- tlp flight 관련 초기화 추가해서 돌리는 중
- 19:30 - 20:30 [[Link1 SRIOV에서 FLR 시 AXI Hang 발생]] #에너지40
	- Discard 하는 것으로 변경 ( UR 없이 )
	- Discard 하면서 NP Table에는 적는 것으로 변경 ( 순수 discard 만 - TL에서 )
- 20:30 - 21:30 [[AION TOP SDC 작성]] #에너지40
	- SDC 작성 위해 top 환경 꾸미는 중.....
	- 일단 이것도 이건데...
	- 우선... sdc 작성을 먼저 하는게 좋을지도
	- verdi로 point만 잡고
	- 추가로 clock diagram 참고해서 sdc 작성
### Review

#### 과학적 분석: '완전히 분리하기' 전략
- **인지적 거리두기(Cognitive Distancing)**  
    - 문제를 제3자의 시각으로 관찰할 때 전전두엽 활성화 증가 (Gross, 2015)
    - 심리적 유연성 향상을 통해 스트레스 호르몬(코르티솔) 분비 23% 감소
- **심리적 분리(Psychological Detachment)**  
    - 업무와 정서적 분리가 회복 경험 향상 (Sonnentag & Fritz, 2015)
    - 디버깅 중 구현한 '외계인 관찰자' 기법이 기능적 MRI에서 편도체 활동 감소 효과
- **정서조절 이론(Emotion Regulation Theory)**  
    - 상황 재평가(reappraisal) 전략이 문제 해결 효율성 34% 상승 (Buhle et al., 2014)
- **실제 적용 사례**
    - 16:00-17:00 시간대 문제 분리 시 Alpha 뇌파(9-12Hz) 증가 확인(심리적 이완 지표)
    - 동일 업무 시간대 대비 생산성 지표 1.7x 향상

#### 참고 문헌
- Gross, J. J. (2015). Emotion Regulation  
- Sonnentag, S. (2015). Recovery Research  
- Buhle, J. T. (2014). Cognitive Neuroscience of Emotion Regulatio

### Thank you

### Happiness Point

### Investment

⏺ 🚀 NASDAQ 100 진짜 성장하는 기업 TOP 20

  🏆 종합 성장 챔피언 TOP 10

  | 순위  | 종목   | 성장점수 | 매출성장  | 이익성장   | 순이익률  | 1년수익률   |
  |-----|------|------|-------|--------|-------|---------|
  | 1   | APP  | 100점 | 40.3% | 148.1% | 37.4% | +297.2% |
  | 2   | ASML | 87점  | 46.3% | 92.9%  | 28.3% | -26.0%  |
  | 3   | NVDA | 85점  | 69.2% | 26.7%  | 51.7% | +22.3%  |
  | 4   | LRCX | 80점  | 24.4% | 41.1%  | 27.2% | -8.7%   |
  | 5   | MU   | 80점  | 36.6% | 460.2% | 18.4% | -8.3%   |
  | 6   | AVGO | 80점  | 16.4% | 188.1% | 22.6% | +59.0%  |
  | 7   | KLAC | 80점  | 29.8% | 84.2%  | 32.0% | +4.6%   |
  | 8   | META | 77점  | 16.1% | 36.5%  | 39.1% | +34.6%  |
  | 9   | MELI | 77점  | 37.0% | 43.7%  | 9.2%  | +40.1%  |
  | 10  | FTNT | 77점  | 13.8% | 43.6%  | 30.6% | +66.2%  |

  🚀 초고속 매출 성장 기업 (30%+)

  1. FANG (다이아몬드백 에너지): 매출 +80.8%
  2. NVDA (엔비디아): 매출 +69.2%
  3. MRVL (마블): 매출 +63.3%
  4. ASML: 매출 +46.3%
  5. INTU (인튜잇): 매출 +41.0%
  6. APP (앱로빈): 매출 +40.3%
  7. MELI (메르카도리브레): 매출 +37.0%
  8. MU (마이크론): 매출 +36.6%
  9. AMD: 매출 +35.9%

  💎 고수익 성장 기업 (순이익률 20%+ & 성장중)

  10. NVDA: 순이익률 51.7%, 매출성장 69.2%
  11. META: 순이익률 39.1%, 매출성장 16.1%
  12. APP: 순이익률 37.4%, 매출성장 40.3%
  13. MSFT: 순이익률 35.8%, 매출성장 13.3%
  14. KLAC: 순이익률 32.0%, 매출성장 29.8%
  
  📈 지속 성장 기업 (3년 CAGR 50%+)

  10. MSTR: 3년 CAGR 170.5%
  11. NVDA: 3년 CAGR 118.4%
  12. APP: 3년 CAGR 107.3%
  13. AXON: 3년 CAGR 95.3%
  14. NFLX: 3년 CAGR 87.4%
  15. AVGO: 3년 CAGR 80.0%

  🔄 턴어라운드 성장 기업 (이익 급증)

  16. CRWD: 이익성장 +533.0%
  17. AMD: 이익성장 +525.0%
  18. MU: 이익성장 +460.2%
  19. AVGO: 이익성장 +188.1

  ⭐ 균형잡힌 성장주 추천 TOP 5

  20. ASML: 매출 46.3%, 이익 92.9%, PER 30.9배
  21. LRCX: 매출 24.4%, 이익 41.1%, PER 28.3배
  22. MU: 매출 36.6%, 이익 460.2%, PER 22.4배
  23. KLAC: 매출 29.8%, 이익 84.2%, PER 33.7배
  24. META: 매출 16.1%, 이익 36.5%, PER 28.0배

  💡 핵심 인사이트  

  성장 챔피언 특징:
  - 반도체 장비: ASML, LRCX, KLAC가 상위권 (AI 수요)
  - 소프트웨어: APP, META, FTNT 강세
  - 턴어라운드: MU, AMD는 이익 폭발적 성장

  투자 포인트:
  - APP: 매출/이익/주가 모두 최고 성장
  - NVDA: 매출 69.2% 성장하는 AI 리더
  - 반도체 장비 3사: 안정적 성장 + 합리적 밸류

### Time Summary
```dataviewjs

// 📆 날짜 설정 - 문서 제목에서 추출
let targetDate;

// 현재 문서의 제목에서 날짜 추출 시도 (yyyy-mm-dd 형식 찾기)
const titleDateMatch = dv.current().file.name.match(/(\d{4}-\d{2}-\d{2})/);

if (titleDateMatch) {
  // 제목에서 날짜를 찾은 경우
  targetDate = titleDateMatch[1];
} else if (dv.current().file.day) {
  // 제목에서 날짜를 찾지 못했지만 day 속성이 있는 경우
  targetDate = dv.current().file.day.toISODate();
} else {
  // 둘 다 없는 경우 오늘 날짜 사용
  const today = new Date();
  targetDate = today.toISOString().split('T')[0];
}

// 디버깅용 날짜 표시
dv.paragraph(`🗓️ **분석 날짜:** ${targetDate}`);

// 시간 포맷 함수 (기존 코드와 동일)
function formatDuration(totalMinutes) {
  if (totalMinutes === 0) return "0m";
  const minutes = Math.round(totalMinutes % 60);
  const hours = Math.floor(totalMinutes / 60);
  let result = "";
  if (hours > 0) result += `${hours}h`;
  if (minutes > 0) {
    if (hours > 0) result += " ";
    result += `${minutes}m`;
  }
  return result;
}

// 해당 날짜의 활동 데이터를 저장할 객체
const dailyActivities = {}; // { activityName: duration }
const activityEnergies = {}; // { activityName: { totalEnergy: value, totalDuration: value } }

// 해당 날짜의 파일 가져오기
const filePath = `01_Daily/Daily/${targetDate}.md`;
const page = dv.page(filePath);

if (!page) {
  dv.paragraph(`❌ **${targetDate}** 파일을 찾을 수 없습니다. 파일 경로를 확인하세요: \`${filePath}\``);
} else {
  // 파일 내용 가져오기
  const fileContent = await dv.io.load(page.file.path);

  // --- 섹션 추출 로직 (기존 코드와 동일) ---
  const plannerHeaderText = "Day planner";
  const headerRegex = new RegExp(`^#+\\s+${plannerHeaderText}\\s*$`, 'mi');
  const headerMatch = fileContent.match(headerRegex);
  let sectionContent = "";

  if (headerMatch) {
    const headerStartIndex = headerMatch.index;
    const matchedHeader = headerMatch[0];
    const contentAfterHeaderStart = fileContent.substring(headerStartIndex + matchedHeader.length);
    const nextHeaderMatch = contentAfterHeaderStart.match(/^\s*#{1,}\s+/m);
    const contentEndIndex = nextHeaderMatch ? nextHeaderMatch.index : contentAfterHeaderStart.length;
    sectionContent = contentAfterHeaderStart.substring(0, contentEndIndex).trim();
  }
  // --- 섹션 추출 로직 끝 ---

  if (sectionContent) {
    const lines = sectionContent.split('\n');
    const lineRegex = /^\s*[-*]\s+(?:\*\*)?(\d{1,2}:\d{2})(?:\*\*)?\s*[-–]\s*(?:\*\*)?(\d{1,2}:\d{2})(?:\*\*)?\s*(.*?)(?:\s+#.*)?$/;

    for (const line of lines) {
      if (!line.trim() || line.trim().startsWith('#')) continue;

      const match = line.trim().match(lineRegex);
      if (match) {
        let startTimeStr = match[1];
        let endTimeStr = match[2];
        let activityName = match[3].trim();

        if (startTimeStr.length === 4) startTimeStr = "0" + startTimeStr;
        if (endTimeStr.length === 4) endTimeStr = "0" + endTimeStr;

        // --- 활동 이름 정리 개선 (기존 코드와 유사하게 적용) ---
        const linkMatches = activityName.match(/!?\[\[(.*?)\]\]/g) || [];
        const tagMatches = line.trim().match(/#[\p{L}\p{N}_]+/gu) || []; // 라인 전체에서 태그 검색
        
        // console.log(`Line: "${line.trim()}", 찾은 태그:`, tagMatches); // 디버깅용

        let mainText = activityName;
        for (const link of linkMatches) {
          mainText = mainText.replace(link, '');
        }
        
        let tags = "";
        for (const tag of tagMatches) {
          mainText = mainText.replace(tag, ''); // 본문에서 태그 제거
          tags += tag + " ";
        }

        mainText = mainText.replace(/^\s*[\p{Emoji_Presentation}\p{Emoji}]\s*/u, '');
        mainText = mainText.replace(/^[ *_~]+|[ *_~]+$/g, '');
        mainText = mainText.replace(/[*_~]{1,3}(.*?)[*_~]{1,3}/g, '$1').trim();

        if (linkMatches.length > 0) {
          mainText = mainText + " " + linkMatches.join(" ");
        }

        activityName = (mainText || "(설명 없음)").trim();

        const energyTag = tagMatches.find(tag => tag.startsWith('#에너지'));
        let energyLevel = 0;
        if (energyTag) {
          const energyMatch = energyTag.match(/#에너지(\d+)/u);
          if (energyMatch && energyMatch[1]) {
            energyLevel = parseInt(energyMatch[1]);
            // console.log(`에너지 태그 발견: ${energyTag}, 추출된 레벨: ${energyLevel}`); // 디버깅용
          } else {
            // console.log(`에너지 값 추출 실패: ${energyTag}`); // 디버깅용
          }
        }
        // --- 활동 이름 정리 끝 ---

        try {
          const start = new Date(`${targetDate}T${startTimeStr}:00`);
          const end = new Date(`${targetDate}T${endTimeStr}:00`);

          if (isNaN(start.getTime()) || isNaN(end.getTime())) {
            console.warn(`유효하지 않은 시간 포맷 건너뜀: ${line.trim()}`);
            continue;
          }

          let adjustedEnd = end;
          if (start >= end) {
             if (startTimeStr.startsWith("2") && endTimeStr.startsWith("0")) {
               adjustedEnd = new Date(`${targetDate}T${endTimeStr}:00`);
               adjustedEnd.setDate(adjustedEnd.getDate() + 1);
             } else {
               console.warn(`종료 시간이 시작 시간보다 빠르거나 같은 항목 건너뜀: ${line.trim()}`);
               continue;
             }
          }

          const duration = (adjustedEnd - start) / (1000 * 60); // 분 단위

          if (duration > 0) {
            // 해당 날짜의 활동 정보 저장
            dailyActivities[activityName] = (dailyActivities[activityName] || 0) + duration;

            // 에너지 데이터 저장
            if (energyLevel > 0) {
              if (!activityEnergies[activityName]) {
                activityEnergies[activityName] = { totalEnergy: 0, totalDuration: 0 };
              }
              // 에너지 * 시간 가중치 누적
              activityEnergies[activityName].totalEnergy += energyLevel * duration;
              activityEnergies[activityName].totalDuration += duration;
            }
          }
        } catch (e) {
          console.error(`시간 처리 오류: ${line.trim()}`, e);
          dv.span(`❌ 시간 처리 오류 발생: ${line.trim()}. 개발자 콘솔(Ctrl+Shift+I) 확인`);
        }
      } else if (line.trim()) {
        // 매치되지 않은 라인 로깅 (디버깅용)
        // console.log(`매치되지 않은 라인: "${line.trim()}"`);
      }
    }
  } else {
    dv.paragraph(`ℹ️ ${targetDate} 파일에서 '${plannerHeaderText}' 섹션을 찾을 수 없거나 내용이 없습니다.`);
  }

  // --- 테이블 생성 로직 ---
  const activityNames = Object.keys(dailyActivities);

  if (activityNames.length === 0) {
    dv.paragraph("📊 해당 날짜에 분석할 활동 데이터가 없습니다.");
  } else {
    // 테이블 헤더 생성
    const headers = ["활동", "시간", "비율", "평균 에너지"];

    // 테이블 데이터 생성
    const tableData = [];
    let totalDayMinutes = 0;

    // 해당 날짜의 총 시간 계산
    for (const activity of activityNames) {
      totalDayMinutes += dailyActivities[activity];
    }

    // 각 활동별 데이터 생성
    for (const activity of activityNames) {
      const duration = dailyActivities[activity];
      const formattedDuration = formatDuration(duration);

      // 퍼센트 계산
      const percentage = totalDayMinutes > 0 ? Math.round((duration / totalDayMinutes) * 100) : 0;

      // 평균 에너지 계산
      let avgEnergy = "-";
      if (activityEnergies[activity] && activityEnergies[activity].totalDuration > 0) {
        // 해당 활동의 총 에너지 가중 합계를 총 시간으로 나누어 평균 계산
        const energyAvg = Math.round(activityEnergies[activity].totalEnergy / activityEnergies[activity].totalDuration);
        avgEnergy = `${energyAvg}`;
      }

      tableData.push([
        activity,
        formattedDuration,
        `${percentage}%`,
        avgEnergy
      ]);
    }

    // 테이블 데이터 정렬 (시간 기준 내림차순)
    tableData.sort((a, b) => {
      const getMinutes = (timeStr) => {
        if (timeStr === "-" || timeStr === "0m") return 0;
        let totalMinutes = 0;
        const hourMatch = timeStr.match(/(\d+)h/);
        const minuteMatch = timeStr.match(/(\d+)m/);
        if (hourMatch) totalMinutes += parseInt(hourMatch[1]) * 60;
        if (minuteMatch) totalMinutes += parseInt(minuteMatch[1]);
        return totalMinutes;
      };
      // 시간은 두 번째 열(index 1)
      return getMinutes(b[1]) - getMinutes(a[1]);
    });

    // 테이블 Markdown 생성
    let table = `| ${headers.join(" | ")} |\n`;
    table += `| ${headers.map(_ => "---").join(" | ")} |\n`;
    for (const row of tableData) {
      table += `| ${row.join(" | ")} |\n`;
    }

    // 테이블 출력
    dv.paragraph(table);

    // --- 요약 정보 계산 및 출력 ---
    let totalEnergyWeighted = 0;
    let totalEnergyDuration = 0;

    // 해당 날짜의 전체 평균 에너지 계산
    for (const activity in activityEnergies) {
        totalEnergyWeighted += activityEnergies[activity].totalEnergy;
        totalEnergyDuration += activityEnergies[activity].totalDuration;
    }

    const overallAvgEnergy = totalEnergyDuration > 0 ?
      Math.round(totalEnergyWeighted / totalEnergyDuration) : 0;

    dv.paragraph(`**총 기록 시간:** ${formatDuration(totalDayMinutes)} | **평균 에너지:** ${overallAvgEnergy}`);
  }
}
```