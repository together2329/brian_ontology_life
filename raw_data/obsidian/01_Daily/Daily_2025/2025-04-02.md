## Plan

- [ ] HCT AON Lint 다시 돌려 보기

- [x] Ratel2 HCT Synthesis Result 검토 ✅ 2025-04-01
	- [ ] IO.IDE 제거 
	
- [ ] 미팅 일정 확인하기 ( Project를 이용해서 Page 만들어 놓기 -> Meeting Minutes 처럼 쓸 수 있게 -> 일정 관리도 하고 )

- [ ] 특허 아이디어 제출 ( UCIe )

위의 task 방식을 적용하는 것이 그냥 적는 것보다는 훨씬 좋음
아니면.... 어딘가에 task를 두고 여기 모아서... filtering 해야 하나...
카테고리 별로 note를 만들고... 거기서 task를 만들고 filtering 해야하나... ( 이 방법은 나중에 연결에 큰 도움이 될지도 모르겠다 )
어떻게든 필터링하고 연결해서 보는 스킬을 늘려 나가자 !!!

---
## Action

* 행동:: 해이 지구랑 산책 + 요거트 식사
	* 세부내용::
	* 리뷰::
	* 시작:: 7:20 
	* 종료:: 8:00
	* 태그:: #행복
	* 에너지::  80 
* 행동::  ExcalDraw + ExcalBrain 공부 ( Visual Thinking 에 대해 익힘 )
	* 세부내용:: Slide Show 및 PDF Export가 자연스럽게 됨. 지식을 정리하기에 최적의 환경
	* 리뷰:: 너무 많은 정보에 에너지가 빠졌지만, 요즘 이 재미로 살고 있음. 제대로 한 번 셋팅해서 해봅시다. ( 강의도 가능함 )
	* 시작:: 8:00 
	* 종료:: 9:20
	* 태그:: #개발환경 
	* 에너지::  80 -> 50
* 행동::  회사에서 커피 한 잔 + 김티엘님과 이야기 + 성능 분석 하며 여유로운 아침
	* 세부내용::
	* 리뷰::
	* 시작:: 9:20 
	* 종료:: 10:00 
	* 태그:: #행복
	* 에너지:: 80 
* 행동:: ip performance 관련 세부 설명 해주기
	* 세부내용:: reordering completion, splitting completion을 AXI ID와 연동해서 설명하기
	* 리뷰:: 이렇게 시나리오 적은 거 어딘가에 한 번 정리해두면 좋을 듯 ( 쭉 연결하는 작업을 주기적으로 해야 내 지식이 단단해짐 )
	* 시작:: 10:00
	* 종료:: 10:30 
	* 태그:: #개발
	* 에너지:: 90
* 행동:: Control SKIP OS 에 대한 논의, Retimer의 Lane Margining에 대한 논의. ( 전TL님으로부터 개념 잘 배웠습니다. )
	* 세부내용:: 
		* 왜 Control SKIP OS가 생겼는가? -> Retimer의 Lane Margining, Parity 등을 측정하기 위해
		* Control SKIP OS는 GEN4 부터 생겼음
		* GEN4-GEN5는 Control SKIP, Strandard SKIP을 번갈아 전송함
		* GEN6는 Control SKIP (40B)만 전송함
	* 리뷰::
	* 시작::  10:30
	* 종료::  11:30
	* 태그::  #회의
	* 에너지::  80
* 행동:: 김TL님과 건강한 밥상! ( 오늘은 식이섬유 먼저 먹는 것을 배움 )
	* 세부내용:: 
	* 리뷰:: 
	* 시작:: 12:00
	* 종료:: 12:30 
	* 태그:: #식사
	* 에너지:: 80
* 행동:: 점심에 행복꾸러미 ( Career 부분 정리하기 ) -> Excal Brian + Excal Draw로 정리해보기 + Project/Kanban 설치만
	* 세부내용:: Career, Fun 부분 다 작성하여 그래프로 연결하였습니다 !!
	* 리뷰:: Confluence 보다 훨씬 빠르고 쉽게 접근할 수 있어 너무 좋습니다.
	* 시작:: 12:30
	* 종료:: 13:00 
	* 태그:: #행복 
	* 에너지:: 80
* 행동:: IDE FM Debugging -> 왜 Fail이 나는지 분석하기
	* 세부내용::
		* IDE TOP까지는 가는데, 나오질 않는다. -> 이 부분 분석 필요함
		* 와... 2시간 30분 걸려서.. Discard 되는 부분 찾음.... ( 어디서 소스인지 다시 분석 필요 )
		* 소스는 Cadence IP Bug 였음. IDE Prefix Offset을 Discard에 잘 못 Assign... -> 수정 후 Pass 확인
	* 리뷰::
	* 시작:: 13:30
	* 종료:: 17:40
	* 태그:: #개발
	* 에너지:: 80
* 행동:: TDISP, Lint, Ratel2 MSI-X, Custom TC, TC Fail 등 한 페이지로 정리해서 ( 프로젝트 + Task로 관리해보기 )
	* 세부내용::
	* 리뷰::
	* 시작::  
	* 종료::  
	* 태그::  
	* 에너지::  
## Time Management

```dataviewjs
const items = dv.current().file.lists;

function extractFieldsAndDetails(children) {
    let detailsArr = [];
    let 리뷰 = "";
    let 시작 = "";
    let 종료 = "";
    let 태그 = "";
    let 에너지 = "";
    
    for (const child of children) {
        const txt = child.text.trim();
        if (txt.startsWith("리뷰::")) {
            리뷰 = txt.replace("리뷰::", "").trim();
        } else if (txt.startsWith("시작::")) {
            시작 = txt.replace("시작::", "").trim();
        } else if (txt.startsWith("종료::")) {
            종료 = txt.replace("종료::", "").trim();
        } else if (txt.startsWith("태그::")) {
            태그 = txt.replace("태그::", "").trim();
        } else if (txt.startsWith("에너지::")) {
            에너지 = txt.replace("에너지::", "").trim();
        } else if (txt.startsWith("세부내용::")) {
            const detailText = txt.replace("세부내용::", "").trim();
            if (detailText !== "") {
                detailsArr.push(detailText);
            }
        } else {
            if (txt !== "") {
                detailsArr.push(txt);
            }
        }
        if (child.children) {
            const subResult = extractFieldsAndDetails(child.children);
            if (subResult.세부내용 !== "") {
                detailsArr.push(subResult.세부내용);
            }
            if (!리뷰 && subResult.리뷰) {
                리뷰 = subResult.리뷰;
            }
            if (!시작 && subResult.시작) {
                시작 = subResult.시작;
            }
            if (!종료 && subResult.종료) {
                종료 = subResult.종료;
            }
            if (!태그 && subResult.태그) {
                태그 = subResult.태그;
            }
            if (!에너지 && subResult.에너지) {
                에너지 = subResult.에너지;
            }
        }
    }
    detailsArr = detailsArr.filter(text => text.length > 0);
    return {
        세부내용: detailsArr.join("<br>"),
        리뷰,
        시작,
        종료,
        태그,
        에너지
    };
}

const logEntries = items
    .filter(item => item.text && item.text.startsWith("행동::"))
    .map(item => {
        const 행동Text = item.text.replace("행동::", "").trim();
        let fields = { 세부내용: "", 리뷰: "", 시작: "", 종료: "", 태그: "", 에너지: "" };
        if (item.children) {
            fields = extractFieldsAndDetails(item.children);
        }
        // "행동", "세부내용", "리뷰" 열: 0.6em, min-width:150px
        const 행동 = `<div style="font-size:0.6em; min-width:150px;">${행동Text}</div>`;
        const 세부내용 = `<div style="font-size:0.6em; min-width:150px;">${fields.세부내용}</div>`;
        const 리뷰 = `<div style="font-size:0.6em; min-width:150px;">${fields.리뷰}</div>`;
        // "시작", "종료", "태그", "에너지" 열: 0.8em, max-width:100px
        const 시작 = `<div style="font-size:0.8em; max-width:100px;">${fields.시작}</div>`;
        const 종료 = `<div style="font-size:0.8em; max-width:100px;">${fields.종료}</div>`;
        const 태그 = `<div style="font-size:0.8em; max-width:100px;">${fields.태그}</div>`;
        const 에너지 = `<div style="font-size:0.8em; max-width:100px;">${fields.에너지}</div>`;
        return [행동, 세부내용, 리뷰, 시작, 종료, 태그, 에너지];
    });

// 헤더에도 동일하게 스타일 적용
const headers = [
    `<div style="font-size:0.6em; min-width:150px;">행동</div>`,
    `<div style="font-size:0.6em; min-width:150px;">세부내용</div>`,
    `<div style="font-size:0.6em; min-width:150px;">리뷰</div>`,
    `<div style="font-size:0.8em; max-width:100px;">시작</div>`,
    `<div style="font-size:0.8em; max-width:100px;">종료</div>`,
    `<div style="font-size:0.8em; max-width:100px;">태그</div>`,
    `<div style="font-size:0.8em; max-width:100px;">에너지</div>`
];

if (logEntries.length > 0) {
    dv.table(headers, logEntries);
} else {
    dv.paragraph("표시할 로그 항목이 없습니다.");
}
```

## Thinking 
## Review 
## Thank you 

