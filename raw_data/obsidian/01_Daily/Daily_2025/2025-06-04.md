### Three Important Goal

1. **Career**: Custom TC Fail 분석 완료 및 Jay 전달 - IDE Regression 진도율 향상
2. **Mind**: AI 도구(Codex, Claude) 학습으로 생산성 시스템 구축 - Life Management System 고도화
3. **Body**: 피부 치료 지속 및 건강 관리 - 꾸준한 치료로 개선 확인

### Day planner

- 7:00 - 7:30 [[일상]] #에너지70 
- 7:30 - 8:45 [[공부]] #에너지70 
	- codex 및 claude로 써보기
	- daily note + investment 등 공부
- 8:45 - 11:30 [[FAIL TC 정리 및 분석]] #에너지70 
	- custom fail tc 분석
	- 정리해서 jay에게 전달
- 11:30 - 12:10 [[식사]] #에너지70 
	- 군만두 + 잡채밥
- 12:10 - 12:30 [[휴식]] #에너지70 
	- 커피 마시며 휴식 - 홍TL님과 산책
- 12:30 - 13:00 [[치료]] #에너지70 
	- 피부 많이 좋아졌고 합니다. 감사합니다.
- 13:00 - 16:00 [[Custom TC Debugging]] #에너지70 
	- custom tc fail 쭉 돌려봄 51개 분석
	- 그 중 바로 확인할 수 있는거 6개 따로 돌림
		- config read write 2개 ( msi copy disable 추가 )
			- msi copy 시 axi master로 tb 가 예상하지 못하는 신호 발생
			- 해당신호 발생하지 않도록 msi copy disable 추가
			- msi copy는 별도 tc에서 검증함
			- 상태: ✅ 해결 완료
		- application error tc 2개
			- issue: Application Error 발생 시 TC Fail
			- root cause: PF3에 Error Injection 후 PF0에서 확인하는 로직 오류
			- solution: 수정 요청함
			- 상태: ✅ 원인 파악 완료, 수정 대기중
		- rx status injection 1개
			- issue: rx status 기반 error check 시 fail
			- 원인: 이미 기존 error count를 고려하지 못함 
			- 진행 상황: init + error injection count로 체크 진행
			- 상태: 🔧 수정 진행중
	- 추가 작업:
		- Grizzly Ridge 종합 task management 시스템 구축
		- 우선순위 매트릭스 (P0/P1/P2) 설정
		- Custom TC 진행률 추적 시스템 구현 (4% → 8%)
		- Daily tracking template 생성
		- Kanban board 개선 (priority 기반 재구성)
		- CLAUDE.md 문서화 업데이트
		- git commit 및 버전 관리
- 16:00 - 17:00 [[미팅 or 세미나]] #에너지70 
	- Transaction Layer 세미나 참석
	- 발표 방식에 대한 피드백 필요
	- PCIe TLP 구조 및 동작 원리 학습
- 17:00 - 17:30 [[미팅 or 세미나]] #에너지70 
	- Transaction Layer 세미나 지속 청취
	- 내용 이해도는 높으나 압축적 전달 능력 개선 필요
	- 핵심 내용을 간결하게 요약하는 스킬 필요


### Review

#### Custom TC Debugging Progress

**전체 현황**: Custom TC Fail 케이스 51개 중 우선순위 6개 선별하여 분석 진행

##### 1. Config Read/Write TC (2개)
**Issue**: MSI Copy 시 AXI Master가 예상치 못한 신호 발생
**Root Cause**: TB가 MSI Copy 동작을 고려하지 않은 검증 로직
**Solution**: MSI Copy 기능 비활성화 후 테스트 진행
**Status**: ✅ 해결 완료
**Note**: MSI Copy 기능은 별도 전용 TC에서 검증 예정

##### 2. Application Error TC (2개)  
**Issue**: Application Error 발생 시 TC Fail
**Root Cause**: PF3에 Error Injection 후 PF0에서 확인하는 로직 오류
**Solution**: 수정 요청함
**Status**: ✅ 원인 파악 완료, 수정 대기중

##### 3. RX Status Injection TC (1개)
**Issue**: RX Status 기반 Error Check 시 Fail 발생
**Root Cause**: 기존 Error Count를 고려하지 않은 검증 로직
**Solution**: Initial Error Count + Injection Count 기반 검증으로 수정
**Status**: 🔧 수정 진행 중

#### 업무 중 인터럽트 상황 대응법

15:00-16:00 시간에 쉬지 못하고 인터럽트 받아 일이 밀렸을 때 행복꾸러미를 참고한 추천 행동:

**즉시 실행 (5-10분)**
- **목욕/세수**: "머리가 맑아지고 정리되는 느낌" - 하루를 새로 시작하는 기분으로 리셋
- **깊은 호흡**: 현재 순간에 집중하며 몸과 마음을 진정시키기

**마음가짐 전환**
- **"이미 완벽"** 혼잣말로 동기부여 - 지금 상황도 괜찮다고 받아들이기
- **순간 소중히 하기**: "예상치 못한 상황에 온전하게 몸을 맡기고 순간을 느끼기"
- 인터럽트도 삶의 일부로 받아들이고 관심과 사랑으로 채우기

**에너지 재집중**
- **우선순위 재정리**: 유한한 에너지를 분산시키지 말고 가장 중요한 일에 집중
- **"될 수 있을까?" 대신 "무조건 된다, 시간의 문제일뿐"** 마인드로 전환
- 밀린 일에 대해 자책하지 말고 바로 행동으로 옮기기

**시스템 활용**
- 할일을 외부 시스템(노트, 앱)에 기록해서 머리 속 부담 덜기
- 단기기억에 의존하지 말고 장기기억으로 연결될 수 있도록 정리

**핵심**: 지금 이 순간부터 "온 에너지를 현재 하고 있는 일에 쏟기"로 다시 시작하기

#### GZR Task

**Grizzly Ridge 전체 Task 목록**

**🔴 Critical (P0) - To do**
1. FLR시 OB Error Response 확인
2. Speed Interrupt 추가 (source + slot 2개)
3. MSG,Mem에 Segment 추가
4. FPGA Timing 분석
5. MSI-X Debugging (MCTP to Host)
6. DUT Error Return
7. Source, Target Speed Interrupt 추가
8. DUT Error Return Issue
9. Requester, Destination Segment 추가
10. SGL - Last BE가 0x3이 아니라 0xF로 나가는 이슈
11. SCAN BIST 신호 추가
12. FLR Issue Follow Up (OB Write, Read Error Response)
13. R017 DB Integration
14. Meeting
15. 업무
16. 미팅 or 세미나
17. R018 DB Integration
18. TL FAIL TC Debugging
19. Fail TC 정리 및 분석
20. FAIL TC 정리 및 분석
21. FPGA FLIT MODE Issue
22. FPGA Dual Port Sim Issue 살펴보기
23. RID Segment Check Logic 구현 (새로 추가)

**🟡 High Priority (P1) - To do**
24. Speed Interrupt 추가 (source + target 2개) - 중복
25. MSG,Mem에 Segment 추가 - 중복
26. Source, Target Speed Interrupt 추가 - 중복
27. Requester, Destination Segment 추가 - 중복

**🟢 Medium Priority (P2) - To do**
28. FPGA Timing 분석 - 중복
29. MSI-X Debugging (MCTP to Host) - 중복
30. AXI Ready Signal Register Slice 추가 (Timing 개선)
31. SGL - Last BE가 0x3이 아니라 0xF로 나가는 이슈 - 중복
32. SCAN BIST 신호 추가 - 중복

**🔄 In-progress**
33. Custom TC Debugging - Config R/W TC (완료)
34. Custom TC Debugging - Application Error TC (원인파악완료, 수정요청함)
35. Custom TC Debugging - RX Status Injection TC (수정중)

**✅ Done**
36. Custom TC 우선순위 6개 선별 및 분석 시작
37. Config Read/Write TC 2개 이슈 해결

**📁 추가 Task 파일 (Kanban에 없음)**
- FPGA에서 IDE TLP 송수신 되지 않음
- Flit Mode IDE TLP RSV != 1 이슈
- IDE Flit Mode Debugging
- IDE L0 Debugging (Additional Fail TC)
- IDE L0L1 Debugging
- IDE Mac Mismatch Debugging
- Requester Segment UR Return 로직 추가

총 30개 이상의 작업이 진행 중이며, 일부 작업이 우선순위별로 중복 등록되어 있습니다.

### Thank you

### Happiness Point

### Time Summary
```dataviewjs

// 📆 날짜 설정 - 문서 제목에서 추출
let targetDate;

// 현재 문서의 제목에서 날짜 추출 시도 (yyyy-mm-dd 형식 찾기)
const titleDateMatch = dv.current().file.name.match(/(\d{4}-\d{2}-\d{2})/);

if (titleDateMatch) {
  // 제목에서 날짜를 찾은 경우
  targetDate = titleDateMatch[1];
} else if (dv.current().file.day) {
  // 제목에서 날짜를 찾지 못했지만 day 속성이 있는 경우
  targetDate = dv.current().file.day.toISODate();
} else {
  // 둘 다 없는 경우 오늘 날짜 사용
  const today = new Date();
  targetDate = today.toISOString().split('T')[0];
}

// 디버깅용 날짜 표시
dv.paragraph(`🗓️ **분석 날짜:** ${targetDate}`);

// 시간 포맷 함수 (기존 코드와 동일)
function formatDuration(totalMinutes) {
  if (totalMinutes === 0) return "0m";
  const minutes = Math.round(totalMinutes % 60);
  const hours = Math.floor(totalMinutes / 60);
  let result = "";
  if (hours > 0) result += `${hours}h`;
  if (minutes > 0) {
    if (hours > 0) result += " ";
    result += `${minutes}m`;
  }
  return result;
}

// 해당 날짜의 활동 데이터를 저장할 객체
const dailyActivities = {}; // { activityName: duration }
const activityEnergies = {}; // { activityName: { totalEnergy: value, totalDuration: value } }

// 해당 날짜의 파일 가져오기
const filePath = `01_Daily/Daily/${targetDate}.md`;
const page = dv.page(filePath);

if (!page) {
  dv.paragraph(`❌ **${targetDate}** 파일을 찾을 수 없습니다. 파일 경로를 확인하세요: \`${filePath}\``);
} else {
  // 파일 내용 가져오기
  const fileContent = await dv.io.load(page.file.path);

  // --- 섹션 추출 로직 (기존 코드와 동일) ---
  const plannerHeaderText = "Day planner";
  const headerRegex = new RegExp(`^#+\\s+${plannerHeaderText}\\s*$`, 'mi');
  const headerMatch = fileContent.match(headerRegex);
  let sectionContent = "";

  if (headerMatch) {
    const headerStartIndex = headerMatch.index;
    const matchedHeader = headerMatch[0];
    const contentAfterHeaderStart = fileContent.substring(headerStartIndex + matchedHeader.length);
    const nextHeaderMatch = contentAfterHeaderStart.match(/^\s*#{1,}\s+/m);
    const contentEndIndex = nextHeaderMatch ? nextHeaderMatch.index : contentAfterHeaderStart.length;
    sectionContent = contentAfterHeaderStart.substring(0, contentEndIndex).trim();
  }
  // --- 섹션 추출 로직 끝 ---

  if (sectionContent) {
    const lines = sectionContent.split('\n');
    const lineRegex = /^\s*[-*]\s+(?:\*\*)?(\d{1,2}:\d{2})(?:\*\*)?\s*[-–]\s*(?:\*\*)?(\d{1,2}:\d{2})(?:\*\*)?\s*(.*?)(?:\s+#.*)?$/;

    for (const line of lines) {
      if (!line.trim() || line.trim().startsWith('#')) continue;

      const match = line.trim().match(lineRegex);
      if (match) {
        let startTimeStr = match[1];
        let endTimeStr = match[2];
        let activityName = match[3].trim();

        if (startTimeStr.length === 4) startTimeStr = "0" + startTimeStr;
        if (endTimeStr.length === 4) endTimeStr = "0" + endTimeStr;

        // --- 활동 이름 정리 개선 (기존 코드와 유사하게 적용) ---
        const linkMatches = activityName.match(/!?\[\[(.*?)\]\]/g) || [];
        const tagMatches = line.trim().match(/#[\p{L}\p{N}_]+/gu) || []; // 라인 전체에서 태그 검색
        
        // console.log(`Line: "${line.trim()}", 찾은 태그:`, tagMatches); // 디버깅용

        let mainText = activityName;
        for (const link of linkMatches) {
          mainText = mainText.replace(link, '');
        }
        
        let tags = "";
        for (const tag of tagMatches) {
          mainText = mainText.replace(tag, ''); // 본문에서 태그 제거
          tags += tag + " ";
        }

        mainText = mainText.replace(/^\s*[\p{Emoji_Presentation}\p{Emoji}]\s*/u, '');
        mainText = mainText.replace(/^[ *_~]+|[ *_~]+$/g, '');
        mainText = mainText.replace(/[*_~]{1,3}(.*?)[*_~]{1,3}/g, '$1').trim();

        if (linkMatches.length > 0) {
          mainText = mainText + " " + linkMatches.join(" ");
        }

        activityName = (mainText || "(설명 없음)").trim();

        const energyTag = tagMatches.find(tag => tag.startsWith('#에너지'));
        let energyLevel = 0;
        if (energyTag) {
          const energyMatch = energyTag.match(/#에너지(\d+)/u);
          if (energyMatch && energyMatch[1]) {
            energyLevel = parseInt(energyMatch[1]);
            // console.log(`에너지 태그 발견: ${energyTag}, 추출된 레벨: ${energyLevel}`); // 디버깅용
          } else {
            // console.log(`에너지 값 추출 실패: ${energyTag}`); // 디버깅용
          }
        }
        // --- 활동 이름 정리 끝 ---

        try {
          const start = new Date(`${targetDate}T${startTimeStr}:00`);
          const end = new Date(`${targetDate}T${endTimeStr}:00`);

          if (isNaN(start.getTime()) || isNaN(end.getTime())) {
            console.warn(`유효하지 않은 시간 포맷 건너뜀: ${line.trim()}`);
            continue;
          }

          let adjustedEnd = end;
          if (start >= end) {
             if (startTimeStr.startsWith("2") && endTimeStr.startsWith("0")) {
               adjustedEnd = new Date(`${targetDate}T${endTimeStr}:00`);
               adjustedEnd.setDate(adjustedEnd.getDate() + 1);
             } else {
               console.warn(`종료 시간이 시작 시간보다 빠르거나 같은 항목 건너뜀: ${line.trim()}`);
               continue;
             }
          }

          const duration = (adjustedEnd - start) / (1000 * 60); // 분 단위

          if (duration > 0) {
            // 해당 날짜의 활동 정보 저장
            dailyActivities[activityName] = (dailyActivities[activityName] || 0) + duration;

            // 에너지 데이터 저장
            if (energyLevel > 0) {
              if (!activityEnergies[activityName]) {
                activityEnergies[activityName] = { totalEnergy: 0, totalDuration: 0 };
              }
              // 에너지 * 시간 가중치 누적
              activityEnergies[activityName].totalEnergy += energyLevel * duration;
              activityEnergies[activityName].totalDuration += duration;
            }
          }
        } catch (e) {
          console.error(`시간 처리 오류: ${line.trim()}`, e);
          dv.span(`❌ 시간 처리 오류 발생: ${line.trim()}. 개발자 콘솔(Ctrl+Shift+I) 확인`);
        }
      } else if (line.trim()) {
        // 매치되지 않은 라인 로깅 (디버깅용)
        // console.log(`매치되지 않은 라인: "${line.trim()}"`);
      }
    }
  } else {
    dv.paragraph(`ℹ️ ${targetDate} 파일에서 '${plannerHeaderText}' 섹션을 찾을 수 없거나 내용이 없습니다.`);
  }

  // --- 테이블 생성 로직 ---
  const activityNames = Object.keys(dailyActivities);

  if (activityNames.length === 0) {
    dv.paragraph("📊 해당 날짜에 분석할 활동 데이터가 없습니다.");
  } else {
    // 테이블 헤더 생성
    const headers = ["활동", "시간", "비율", "평균 에너지"];

    // 테이블 데이터 생성
    const tableData = [];
    let totalDayMinutes = 0;

    // 해당 날짜의 총 시간 계산
    for (const activity of activityNames) {
      totalDayMinutes += dailyActivities[activity];
    }

    // 각 활동별 데이터 생성
    for (const activity of activityNames) {
      const duration = dailyActivities[activity];
      const formattedDuration = formatDuration(duration);

      // 퍼센트 계산
      const percentage = totalDayMinutes > 0 ? Math.round((duration / totalDayMinutes) * 100) : 0;

      // 평균 에너지 계산
      let avgEnergy = "-";
      if (activityEnergies[activity] && activityEnergies[activity].totalDuration > 0) {
        // 해당 활동의 총 에너지 가중 합계를 총 시간으로 나누어 평균 계산
        const energyAvg = Math.round(activityEnergies[activity].totalEnergy / activityEnergies[activity].totalDuration);
        avgEnergy = `${energyAvg}`;
      }

      tableData.push([
        activity,
        formattedDuration,
        `${percentage}%`,
        avgEnergy
      ]);
    }

    // 테이블 데이터 정렬 (시간 기준 내림차순)
    tableData.sort((a, b) => {
      const getMinutes = (timeStr) => {
        if (timeStr === "-" || timeStr === "0m") return 0;
        let totalMinutes = 0;
        const hourMatch = timeStr.match(/(\d+)h/);
        const minuteMatch = timeStr.match(/(\d+)m/);
        if (hourMatch) totalMinutes += parseInt(hourMatch[1]) * 60;
        if (minuteMatch) totalMinutes += parseInt(minuteMatch[1]);
        return totalMinutes;
      };
      // 시간은 두 번째 열(index 1)
      return getMinutes(b[1]) - getMinutes(a[1]);
    });

    // 테이블 Markdown 생성
    let table = `| ${headers.join(" | ")} |\n`;
    table += `| ${headers.map(_ => "---").join(" | ")} |\n`;
    for (const row of tableData) {
      table += `| ${row.join(" | ")} |\n`;
    }

    // 테이블 출력
    dv.paragraph(table);

    // --- 요약 정보 계산 및 출력 ---
    let totalEnergyWeighted = 0;
    let totalEnergyDuration = 0;

    // 해당 날짜의 전체 평균 에너지 계산
    for (const activity in activityEnergies) {
        totalEnergyWeighted += activityEnergies[activity].totalEnergy;
        totalEnergyDuration += activityEnergies[activity].totalDuration;
    }

    const overallAvgEnergy = totalEnergyDuration > 0 ?
      Math.round(totalEnergyWeighted / totalEnergyDuration) : 0;

    dv.paragraph(`**총 기록 시간:** ${formatDuration(totalDayMinutes)} | **평균 에너지:** ${overallAvgEnergy}`);
  }
}
```