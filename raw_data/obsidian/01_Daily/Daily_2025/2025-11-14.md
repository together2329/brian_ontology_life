### Three Important Goal

Narrow Burst Support ! 
Read Interleaving ! 

Security Handler ( Async FIFO Update )

**Firenze SDC Update**   
Async FIFO Path

- data path 2 cycle
- pointer path 1 cycle  
    ASIC team에도 문의 하기

Open Edge , ADB400 방식 모두 분석 후 좋은 방식을 선택해서 진행하기
이와 관련해서 월요일에 팀에 관련 내용 공유하면 좋을듯 

AI 분야 수요 조사 ( 팀원들 한명씩 물어보고 정리 후 송부 )
- RTL Coding 분석
- 간단한 RTL Coding 및 이슈 확인
- pcie spec 에서 필요한 내용 찾기 ( 빠름 )
- 영어 번역 및 작문 

ATC ( rdata로 구분 가능 할 듯 - all 0xFFFF_FFFF , or 0x0 )

Lint 확인 필요 ( LC_ENABLE - all condition is not assigned )

MSG Handler Lint 수정 알려주기 ( RHS , LHS )
- OHC Parsing 문제 없는지 확인
- 그리고 length shift 안 짤리는지 확인. bit 늘리는거 필요한지? ATS처럼

FRS 확인 필요 ( 종호TL님에게 CL# 알려주기 )
FRS 확인하는 것 켜두기 ( custom tc 요청 사항 추가하기 )

SRIOV - VF Enable/Disable 관련 FRS가 Oubound Open 후에 나감
- 수정 완료 - sim으로 확인 필요 
- 종호TL님에게 답변 필요

CLD Clarification 
- OHC 포함됨 ( ATS MSG에 )
- 그리고 .. ATS REQ, PRI RESP 2개다 구분 필요한지.
- 아니면 하나 더 만들어야 할지. Msg Code Decoding만 
- OHC Parsing만 추가로 할지 고민 ( 또 한 가지 모드가 됨 )

CLD 쪽에 DOE Event 개수 알려주기 ( 15개 )

HLD OHC Parsing 논의 

AXI Master에서 parameter 통해서 grizzly ridge, firenze address 다르게 주기
그리고 register default 변경 통해서 Project 다르게 관리
내부는 통일하기 ( 똑같도록 만들기 )

Address Translation Guide 설정 해두기 ( 명재TL님 요청 )

ATS Msg ( OHC Parsing 필요함 )
- 수정 완료 
- 일단 OHC 없이 잘 되는 것은 확인 ( ohc 포함 확인 필요 )

msg handler 옮겨두기 ( HLD로 - 완료 )
형근TL님에게 알려주기

IDO Guide에, k_select 가이드 추가 

TDI Error Interrupt 현재 구현으로 괜찮을지. Grouping 부분.
- IDE Link down ( falling edge 만들어서 register 초기화 하도록 변경 필요 할 듯 )
- FLR 때는.. 흠.. 


AHEAD IV 구현 ( 승훈 TL님 구현 완료 )  
- AHEAD Limit 관련 rollback option 추가
- SEC REG 에 Enable W1C 변경
- CSR 생성 필요
- CSR 추가 필요 ( ATS TEE - all pf , all vf )
- async fifo 관련 src, dst 알려주기 

ATS TEE bit 추가 완료
- all pf
- all vf

DOE Debugging 추가 ( mailbox depth , status )
DOE Control로 status 움직이는 것도 ? 

---

**HLD Event 작업 필요 ( port는 만들었고, 연결할 필요가 있음 )**
- mctp intr ( sfr에서 output으로 뽑자 )
- doe intr ( ip reg에서 뽑자. GZR 먼저 바꾸고, Firenze 바꾸고, HLD 변경 )
	- 5 bit을 muxing 하는 옵션 추가하기 ( 하위 5 bit에 )

ATS TEE bit 추가 필요함
- TEE ( dev cap 30 bit에 연동해서 ATS TEE, Global Invalidate Supported 확인 필요 )

TEE Supported 가 1이고, Default Stream이 아닐 경우,  
rid 기반 UR, rid 기반 unexpected completion 처리.  
필요할지. 없다면 그냥 skip

ATC ( rdata로 구분 가능 할 듯 - all 0xFFFF_FFFF , or 0x0 )

**Read Interleaving Issue 해결하기**
- ID Hooking 해서 진행하는 방식은 ?? 
- ID 32 depth 저장 후, 0 tie 후 전송
- 그리고 받아서 다시 switching

MCTP 는 이제 계속 보완 ( hct sub level 에서 plus argument로 보완 , checker 보완 )
MCTP hct sub code 공유 ( to yossep )
TDISP 는 roll back option 
TDISP SV code, hct sub code 공유 ( to viktor )
MCTP, TDISP 관련 코드 리뷰 필요 ( FW 팀과 함께 논의 필요 )
Spec의 어디를 참조하였는지 확인 필요 ( FW 팀과 함께 논의 필요 )

AXI to SRAM, MSG Reciver 쪽도 확인 필요
obc에서 Shared SRAM을 적극적으로 사용했을 때 예외처리 빠진게 없을지?

TL, HAL 변경점 확인 ( Spec 상에서 )
IDE 변경점 확인 ( Spec 상에서 )
TL, HAL GEN6 IP 분석 ( 시작 필요 )
IDE GEN6 IP 분석 ( 거의 완료 )

UCIe IP 내부. 이거 자체가 잘 동작하는 것에 집중하는 것도 중요함.
### Day planner

- 8:40 - 11:40 [[업무]] #에너지80 
	- viktor와 tdisp 관련 sequence 논의 ( simple tc 완료 )
	- dve command 배우기 ( 이걸 사용하면 debugging이 쉬워짐 )
	- verdi 도 가능함
	- konstantin에게 call back 관련 지식 배우기
- 10:00 - 11:40 [[TDISP + XT Mode 확인]] #에너지80 
	- AEAD LIMIT Feature Review + Submit
		- secure이 아닐 때 set 되도록 변경
		- secure 일 때 set 하면 fail 나도록 변경 ( 범용적으로 사용 가능 할 듯 )
	- TC Requirement 요청하기 ( Viktor에게 )
- 11:40 - 12:30 [[식사]] #에너지80 
- 12:00 - 12:30 [[산책]] #에너지80 
- 12:30 - 13:00 [[TDISP + XT Mode 확인]] #에너지80 
	- AEAD LIMIT Feature Review + Submit
- 13:00 - 14:37 [[TDISP + XT Mode 확인]] #에너지80 
	- ATS TEE PF에서 조건 변경 ( PF0 -> All FUNC )
	- VF에 ATS TEE 추가
- 14:37 - 15:43 [[Firenze ATS PRI RX Logic ( Direct Access )]] #에너지80 
	- ATS RX 시 OHC Parsing 부분 추가 완료함
	- OHC 여러 케이스 검증 필요 + Mem Write랑 섞여서 오는 케이스 검증 필요
- 15:43 - 18:00 [[업무]]
	-  HLD MSG Handler 반영
	- Simulation 필요
- 18:00 - 18:30 [[식사]]
	- 아귀찜
- 18:30 - 19:15 [[업무]]
	- grizzly ridge 반영
		- ats tee bit 반영
		- aead limit detectino 반영
### Review

#### 📊 오늘의 성과
**주요 완료 작업**:
- ✅ AEAD LIMIT Feature Review + Submit (secure/non-secure 조건 구현)
- ✅ ATS TEE PF 조건 변경 (PF0 → All FUNC), VF에 ATS TEE 추가
- ✅ Firenze ATS RX - OHC Parsing 부분 추가 완료
- ✅ HLD MSG Handler 반영 완료
- ✅ Grizzly Ridge에 ATS TEE bit, AEAD limit detection 반영

**기술 학습**:
- DVE command 사용법 배움 (디버깅 효율 향상)
- Verdi 활용 가능성 확인
- Konstantin에게 call back 관련 지식 습득

**협업 성과**:
- Viktor와 TDISP sequence 논의, simple TC 완료
- Viktor에게 TC Requirement 요청
- 여러 모듈 통합 진행 (HLD, Firenze, Grizzly Ridge)

#### 🎯 에너지 관리
- 오늘은 대부분 **에너지 80**으로 안정적 유지
- 집중도 높은 하루, 큰 에너지 저하 없음
- 점심 후 산책으로 리프레시 효과

#### 🔍 다음 단계
**검증 필요**:
- OHC 여러 케이스 검증 (Mem Write와 혼합된 케이스)
- HLD MSG Handler Simulation 진행
- Async FIFO Path SDC 작업 (월요일 팀 공유 예정)

**후속 작업**:
- AI 분야 수요 조사 (팀원들에게 질의)
- Async FIFO: Open Edge vs ADB400 방식 비교 분석

#### 💡 인사이트
- 여러 모듈 동시 작업으로 통합 능력 향상
- 디버깅 도구 학습으로 효율성 개선 기대
- 협업 통한 지식 공유 활발 (Viktor, Konstantin)

### Thank you

### Happiness Point

### Time Summary
```dataviewjs

// 📆 날짜 설정 - 문서 제목에서 추출
let targetDate;

// 현재 문서의 제목에서 날짜 추출 시도 (yyyy-mm-dd 형식 찾기)
const titleDateMatch = dv.current().file.name.match(/(\d{4}-\d{2}-\d{2})/);

if (titleDateMatch) {
  // 제목에서 날짜를 찾은 경우
  targetDate = titleDateMatch[1];
} else if (dv.current().file.day) {
  // 제목에서 날짜를 찾지 못했지만 day 속성이 있는 경우
  targetDate = dv.current().file.day.toISODate();
} else {
  // 둘 다 없는 경우 오늘 날짜 사용
  const today = new Date();
  targetDate = today.toISOString().split('T')[0];
}

// 디버깅용 날짜 표시
dv.paragraph(`🗓️ **분석 날짜:** ${targetDate}`);

// 시간 포맷 함수 (기존 코드와 동일)
function formatDuration(totalMinutes) {
  if (totalMinutes === 0) return "0m";
  const minutes = Math.round(totalMinutes % 60);
  const hours = Math.floor(totalMinutes / 60);
  let result = "";
  if (hours > 0) result += `${hours}h`;
  if (minutes > 0) {
    if (hours > 0) result += " ";
    result += `${minutes}m`;
  }
  return result;
}

// 해당 날짜의 활동 데이터를 저장할 객체
const dailyActivities = {}; // { activityName: duration }
const activityEnergies = {}; // { activityName: { totalEnergy: value, totalDuration: value } }

// 해당 날짜의 파일 가져오기
const filePath = `01_Daily/Daily/${targetDate}.md`;
const page = dv.page(filePath);

if (!page) {
  dv.paragraph(`❌ **${targetDate}** 파일을 찾을 수 없습니다. 파일 경로를 확인하세요: \`${filePath}\``);
} else {
  // 파일 내용 가져오기
  const fileContent = await dv.io.load(page.file.path);

  // --- 섹션 추출 로직 (기존 코드와 동일) ---
  const plannerHeaderText = "Day planner";
  const headerRegex = new RegExp(`^#+\\s+${plannerHeaderText}\\s*$`, 'mi');
  const headerMatch = fileContent.match(headerRegex);
  let sectionContent = "";

  if (headerMatch) {
    const headerStartIndex = headerMatch.index;
    const matchedHeader = headerMatch[0];
    const contentAfterHeaderStart = fileContent.substring(headerStartIndex + matchedHeader.length);
    const nextHeaderMatch = contentAfterHeaderStart.match(/^\s*#{1,}\s+/m);
    const contentEndIndex = nextHeaderMatch ? nextHeaderMatch.index : contentAfterHeaderStart.length;
    sectionContent = contentAfterHeaderStart.substring(0, contentEndIndex).trim();
  }
  // --- 섹션 추출 로직 끝 ---

  if (sectionContent) {
    const lines = sectionContent.split('\n');
    const lineRegex = /^\s*[-*]\s+(?:\*\*)?(\d{1,2}:\d{2})(?:\*\*)?\s*[-–]\s*(?:\*\*)?(\d{1,2}:\d{2})(?:\*\*)?\s*(.*?)(?:\s+#.*)?$/;

    for (const line of lines) {
      if (!line.trim() || line.trim().startsWith('#')) continue;

      const match = line.trim().match(lineRegex);
      if (match) {
        let startTimeStr = match[1];
        let endTimeStr = match[2];
        let activityName = match[3].trim();

        if (startTimeStr.length === 4) startTimeStr = "0" + startTimeStr;
        if (endTimeStr.length === 4) endTimeStr = "0" + endTimeStr;

        // --- 활동 이름 정리 개선 (기존 코드와 유사하게 적용) ---
        const linkMatches = activityName.match(/!?\[\[(.*?)\]\]/g) || [];
        const tagMatches = line.trim().match(/#[\p{L}\p{N}_]+/gu) || []; // 라인 전체에서 태그 검색
        
        // console.log(`Line: "${line.trim()}", 찾은 태그:`, tagMatches); // 디버깅용

        let mainText = activityName;
        for (const link of linkMatches) {
          mainText = mainText.replace(link, '');
        }
        
        let tags = "";
        for (const tag of tagMatches) {
          mainText = mainText.replace(tag, ''); // 본문에서 태그 제거
          tags += tag + " ";
        }

        mainText = mainText.replace(/^\s*[\p{Emoji_Presentation}\p{Emoji}]\s*/u, '');
        mainText = mainText.replace(/^[ *_~]+|[ *_~]+$/g, '');
        mainText = mainText.replace(/[*_~]{1,3}(.*?)[*_~]{1,3}/g, '$1').trim();

        if (linkMatches.length > 0) {
          mainText = mainText + " " + linkMatches.join(" ");
        }

        activityName = (mainText || "(설명 없음)").trim();

        const energyTag = tagMatches.find(tag => tag.startsWith('#에너지'));
        let energyLevel = 0;
        if (energyTag) {
          const energyMatch = energyTag.match(/#에너지(\d+)/u);
          if (energyMatch && energyMatch[1]) {
            energyLevel = parseInt(energyMatch[1]);
            // console.log(`에너지 태그 발견: ${energyTag}, 추출된 레벨: ${energyLevel}`); // 디버깅용
          } else {
            // console.log(`에너지 값 추출 실패: ${energyTag}`); // 디버깅용
          }
        }
        // --- 활동 이름 정리 끝 ---

        try {
          const start = new Date(`${targetDate}T${startTimeStr}:00`);
          const end = new Date(`${targetDate}T${endTimeStr}:00`);

          if (isNaN(start.getTime()) || isNaN(end.getTime())) {
            console.warn(`유효하지 않은 시간 포맷 건너뜀: ${line.trim()}`);
            continue;
          }

          let adjustedEnd = end;
          if (start >= end) {
             if (startTimeStr.startsWith("2") && endTimeStr.startsWith("0")) {
               adjustedEnd = new Date(`${targetDate}T${endTimeStr}:00`);
               adjustedEnd.setDate(adjustedEnd.getDate() + 1);
             } else {
               console.warn(`종료 시간이 시작 시간보다 빠르거나 같은 항목 건너뜀: ${line.trim()}`);
               continue;
             }
          }

          const duration = (adjustedEnd - start) / (1000 * 60); // 분 단위

          if (duration > 0) {
            // 해당 날짜의 활동 정보 저장
            dailyActivities[activityName] = (dailyActivities[activityName] || 0) + duration;

            // 에너지 데이터 저장
            if (energyLevel > 0) {
              if (!activityEnergies[activityName]) {
                activityEnergies[activityName] = { totalEnergy: 0, totalDuration: 0 };
              }
              // 에너지 * 시간 가중치 누적
              activityEnergies[activityName].totalEnergy += energyLevel * duration;
              activityEnergies[activityName].totalDuration += duration;
            }
          }
        } catch (e) {
          console.error(`시간 처리 오류: ${line.trim()}`, e);
          dv.span(`❌ 시간 처리 오류 발생: ${line.trim()}. 개발자 콘솔(Ctrl+Shift+I) 확인`);
        }
      } else if (line.trim()) {
        // 매치되지 않은 라인 로깅 (디버깅용)
        // console.log(`매치되지 않은 라인: "${line.trim()}"`);
      }
    }
  } else {
    dv.paragraph(`ℹ️ ${targetDate} 파일에서 '${plannerHeaderText}' 섹션을 찾을 수 없거나 내용이 없습니다.`);
  }

  // --- 테이블 생성 로직 ---
  const activityNames = Object.keys(dailyActivities);

  if (activityNames.length === 0) {
    dv.paragraph("📊 해당 날짜에 분석할 활동 데이터가 없습니다.");
  } else {
    // 테이블 헤더 생성
    const headers = ["활동", "시간", "비율", "평균 에너지"];

    // 테이블 데이터 생성
    const tableData = [];
    let totalDayMinutes = 0;

    // 해당 날짜의 총 시간 계산
    for (const activity of activityNames) {
      totalDayMinutes += dailyActivities[activity];
    }

    // 각 활동별 데이터 생성
    for (const activity of activityNames) {
      const duration = dailyActivities[activity];
      const formattedDuration = formatDuration(duration);

      // 퍼센트 계산
      const percentage = totalDayMinutes > 0 ? Math.round((duration / totalDayMinutes) * 100) : 0;

      // 평균 에너지 계산
      let avgEnergy = "-";
      if (activityEnergies[activity] && activityEnergies[activity].totalDuration > 0) {
        // 해당 활동의 총 에너지 가중 합계를 총 시간으로 나누어 평균 계산
        const energyAvg = Math.round(activityEnergies[activity].totalEnergy / activityEnergies[activity].totalDuration);
        avgEnergy = `${energyAvg}`;
      }

      tableData.push([
        activity,
        formattedDuration,
        `${percentage}%`,
        avgEnergy
      ]);
    }

    // 테이블 데이터 정렬 (시간 기준 내림차순)
    tableData.sort((a, b) => {
      const getMinutes = (timeStr) => {
        if (timeStr === "-" || timeStr === "0m") return 0;
        let totalMinutes = 0;
        const hourMatch = timeStr.match(/(\d+)h/);
        const minuteMatch = timeStr.match(/(\d+)m/);
        if (hourMatch) totalMinutes += parseInt(hourMatch[1]) * 60;
        if (minuteMatch) totalMinutes += parseInt(minuteMatch[1]);
        return totalMinutes;
      };
      // 시간은 두 번째 열(index 1)
      return getMinutes(b[1]) - getMinutes(a[1]);
    });

    // 테이블 Markdown 생성
    let table = `| ${headers.join(" | ")} |\n`;
    table += `| ${headers.map(_ => "---").join(" | ")} |\n`;
    for (const row of tableData) {
      table += `| ${row.join(" | ")} |\n`;
    }

    // 테이블 출력
    dv.paragraph(table);

    // --- 요약 정보 계산 및 출력 ---
    let totalEnergyWeighted = 0;
    let totalEnergyDuration = 0;

    // 해당 날짜의 전체 평균 에너지 계산
    for (const activity in activityEnergies) {
        totalEnergyWeighted += activityEnergies[activity].totalEnergy;
        totalEnergyDuration += activityEnergies[activity].totalDuration;
    }

    const overallAvgEnergy = totalEnergyDuration > 0 ?
      Math.round(totalEnergyWeighted / totalEnergyDuration) : 0;

    dv.paragraph(`**총 기록 시간:** ${formatDuration(totalDayMinutes)} | **평균 에너지:** ${overallAvgEnergy}`);
  }
}
```