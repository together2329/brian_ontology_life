### Three Important Goal

- Debugging TC 확인
- Error Injection TC 
- Performance BackPressure
- IDE Cap Reset 되도록 추가 ( Perst )
- IDE Reset TC 요청하기
- Event Handler Polling 이슈 확인
- AI 과제 계정 생성하기 ( Chiplet ) - Storage 빵빵하게

### Day planner

- 7:00 - 8:20 [[식사]] #에너지70 
	- 들깨 순두부 + LA갈비 + 반찬 + 밥
- 8:20 - 9:20 [[일상]] #에너지70 
	- 나갈 준비 !
- 9:20 - 10:50 [[일상]] #에너지70
	- 부모님 수원역에 데려다 드리고 , 회사 출근
- 10:50 - 12:00 [[업무]] #에너지50 
	- 오랜만에 출근해서 email 읽기 
	- 약간 멍때림
- 12:00 - 12:30 [[식사]]  #에너지50 
	- 비빔국수 + 불고기
- 12:30 - 13:30 [[Life Management Tool 개발]] #에너지50
	- 뭔가 AI tool 자체 보다는 obsidian 에 어떻게 기록하고 나의 지식을 확장 시켜나갈지가 중요
- 13:30 - 15:00 [[Requester, Destination Segment 추가]] #에너지50
	- link wrapper 0, 1 에 추가된 부분 다시 한 번 검토 후
	- ide simulation 진행 중
- 15:00 - 16:20 [[미팅 or 세미나]] #에너지70 
	- Data Link Layer 세미나
		- Overview 
		- 내꺼, 니꺼 ( host, device 로 설명이 좋을 듯 )
		- flit mode, non flit mode dllp 필드 설명 ( SD - shared, dedicated )
		- Header FC, Data FC가 0이면 무슨 의미? ( flit mode까지 고려해서 )
		- PA 에서 DLLP 관련 동작 교환 예시
		- Flit Mode에서 scale 기능 필수 인가?
		- 12bit인데 왜 2048인가? -> 1bit은 empty나 overflow를 위해 활용하기 때문
		- Replay Timer는 언제 증가 시키나? 왜 증가 시키나?
		- Replay Num은 왜 2씩 증가하나? & Replay Num은 언제 1로 돌아오나?
			- 1씩 증가하는 경우 ( 64G ) -> Speed Down -> 2씩 증가 ( 32G )
		- LCRC is logical NOT -> 설명
		- 플로우 차트 설명 때 확대하거나, 글씨가 흰색이 아니면 더 좋을듯
		- Repaly Command에서 전송된 Flit 숫자가 아니라 자신의 숫자 
- 16:20 - 17:00 [[Requester, Destination Segment 추가]]
	- flit mode enable을 제외해야 NFM FM 모두 쓸 수 있을듯 - 아마 안 에서 내부적으로 되는듯
	- IDE Test 다시 진행해보기
- 17:00 - 17:45 [[Life Management Tool 개발]]
	- 있는 툴을 최대한 활용
		- Notebook LM -> 행복꾸러미 -> Daily Note
		- Notebook LM -> 일 잘하는 방법 -> Daily Note
		- GPT Project -> 행복꾸러미 -> Daily Note
		- GPT Project -> 일 잘하는 방법 -> Daily Note
		- python or javascript 기반으로 api 호출
- 18:30 - 19:30 [[일상]]
	- 마음이 가장 중요하다
	- 감사하고 무한대의 힘을 느끼는 것
	- 그리고 조급해 하지 않는 것
	- 계속 발굴하고 실천해 나가는 것
- 22:00 - 22:40 [[Life Management Tool 개발]]
	- 모든 무료 툴 활용하기
		- 방도가 없음 -> 계속 질문하고 옵시디언에 저장하고 연결하기 ( 이걸 정리해서 핵심만 정리하기 - clipping 자체는 없애기 )
		- 하루 1시간 매일 매일 경제 공부하기
		- 하루 1시간 정도는 PCIe + UCIe Spec 공부하기
		- 하루 1시간 정도는 설계 공부하기
		- 질문을 만들어 내고 정리하기
		- 경제 사이트에서 정보를 얻기
		- 유튜브에서 정보를 얻기
		- 미친듯이 공부하기
		- 사람들의 불편함을 얻기 -> 이걸 해결하기 -> 반복
	- 시스템 프롬프트와 Context 를 만들어 내기
		- excalidraw + excalibrain 등으로 구상하기
	- 맥락을 만들어내기 ( 파이썬으로 )
		- 옵시디언 안에서 자료를 모으고 맥락을 만들어 내고
		- 상용 툴의 도움을 받자
		- 추가로 발전하면 open ai o3 250$ 결제로 시행
	- 맥락을 만들고, gpt + gemini + notebooklm의 도움을 받아서 내가 몰입할 수 있는 환경 만든다
	- 맥락을 자동으로 만드는 템플릿 만들고 답변 받으면 그걸 또 맥락으로 연결하기
	- 지금은 Grizzly Ridge + AIONHLD 에 집중해야 할 때
		- Cube방의 내용 모두 타이핑하고 흐름 따라가기
		- Email 내용 모두 타이핑하고 흐름 따라가기
		- NXP Datasheet 보고 어떻게 구성할지 공부하기 ( PDF )
	- Verilog + UVM 등을  gemini + playground로 공부해나가자
	- planning 하기 ( 일주일 )
	- 목표 계속 생각하기 ( 알면서도 알지 못하는 것들 )
	- 돈의 힘 다시 읽기
	- 모닝글로리 투자법 다시 보기

### Review

### Thank you

### Happiness Point

제공해주신 일과 기록을 바탕으로 소스와의 연관성을 살펴보겠습니다.

**7:00 - 8:20 식사** #에너지70 **12:00 - 12:30 식사** #에너지50 식사는 일상적인 활동입니다. 소스에서는 '소식하는 것을 좋아한다'는 내용이 언급되며, 이를 통해 몸과 마음이 편안함을 느낄 수 있다고 합니다. 건강한 식사를 하는 것에 대한 긍정적인 언급도 있습니다.

**8:20 - 9:20 일상 (준비)** #에너지70 **9:20 - 10:50 일상 (부모님 모셔다 드림)** #에너지70 소중한 사람들에게 사랑과 고마움을 표현하는 것은 큰 행복이라고 소스에서 강조됩니다. 가족과의 관계를 소중히 여기고 연락하는 것, 그리고 부모님을 돕는 것 등은 삶의 풍요로움과 큰 기쁨을 줍니다. 부모님을 수원역에 모셔다 드린 활동은 이러한 **가족과의 관계를 소중히 여기는 가치**와 연결될 수 있습니다.

**10:50 - 12:00 업무 (메일 확인, 멍때림)** #에너지50 업무 시간에 '약간 멍때림'이 있었다는 것은, 소스에서 강조하는 '온 에너지를 현재 하고 있는 일에 집중하는 것'과는 다소 거리가 있어 보입니다. 소스에 따르면, 회사에 있는 동안은 회사를 위해 무엇을 할 수 있을지만 생각하고 다른 생각은 하지 않는 것이 회사에서 행복할 수 있는 방법이라고 합니다. 에너지 집중의 중요성이 여러 차례 언급됩니다. 목표에 집중하지 않으면 이루기 어렵다는 내용도 있습니다.

**12:30 - 13:30 Life Management Tool 개발** #에너지50 이 활동은 '새로운 것을 배울 때' 느끼는 즐거움 및 '시스템을 활용하여 목표를 계속 내 머릿속에 각인시키는 것'과 연결될 수 있습니다. 소스에서는 Confluence/Jira/Tempo/Team Calendar/Google Drive와 같은 시스템을 사용하는 것을 언급하며, 이를 통해 누군가의 삶에 변화를 일으키거나 목표 달성을 위해 생각을 집중시키는 것의 중요성을 이야기합니다. Obsidian을 활용하여 지식을 확장하는 것은 **학습하고 성장하며 시스템을 구축하는 것**과 관련이 깊습니다.

**13:30 - 15:00 Requester, Destination Segment 추가 (SoC 개발)** #에너지50 **16:20 - 17:00 Requester, Destination Segment 추가 (SoC 개발)** 이것은 핵심적인 SoC 개발 업무입니다. 소스에서 SoC 개발을 하면서 느끼는 즐거움이 상세히 설명되어 있습니다. 특히 '직접 설계하고 개발할 때' 즐거움을 느끼며, PCIe와 같은 IP를 공부하고 다루는 것 역시 즐거움의 요소입니다. Requester, Destination Segment를 추가하고 IDE 시뮬레이션 및 테스트를 진행하는 것은 **SoC 개발의 직접적인 설계 및 개발 활동**에 해당하며, 이는 소스에서 언급된 **어려운 문제를 해결하고 IP/Spec을 공부하는 즐거움**과 직접적으로 연결됩니다. 처음 이 업무를 시작했을 때 에너지 레벨이 50이었지만, 세미나 이후 같은 업무를 할 때는 70으로 상승한 점이 주목됩니다.

**15:00 - 16:20 미팅 or 세미나 (Data Link Layer 세미나)** #에너지70 세미나 참석은 '새로운 것을 배울 때' 느끼는 즐거움 및 '공부한 것을 잘 이해해서 세미나 진행할 때 (알려줄 때)' 느끼는 즐거움과 매우 밀접하게 관련됩니다. 소스에서는 PCIe와 CXL 세미나를 할 기회가 있는 것을 즐거움으로 언급하며, 사람들한테 지식을 이해하기 쉽게 전달하는 것을 좋아한다고 합니다. 또한, 발표하는 것을 좋아하며 열심히 준비한다고 언급됩니다. 세미나 내용을 상세히 기록하고 이해하려는 노력(예: 질문 목록)은 **지식 습득 및 공유에 대한 열정**을 보여주며, 이는 소스에서 언급된 **성장하고 새로운 것을 배우며 사람들에게 지식을 전달하는 즐거움**과 일맥상통합니다. 이 활동 중에 에너지 레벨이 50에서 70으로 상승한 것은 이러한 활동이 사용자에게 긍정적인 에너지를 준다는 것을 시사하며, 이는 소스에서 **특정 활동들이 에너지를 높여준다**는 내용과도 부합합니다.

요약하자면, 제공해주신 일과 기록에는 **SoC 개발 관련 업무, 학습 및 지식 공유 활동, 그리고 삶의 시스템 구축 노력** 등 소스에서 언급된 **사용자가 즐거움과 에너지를 느끼는 핵심적인 활동들**이 포함되어 있습니다. 또한 **가족과의 관계를 소중히 여기는 모습**도 드러나 있습니다. 일과 중 에너지 레벨의 변화는 이러한 활동들이 사용자에게 미치는 영향을 보여주는 데이터로 해석될 수 있습니다. 특히 세미나 참석과 같은 학습/공유 활동 후 에너지 레벨이 상승한 점이 눈에 띕니다.

### Time Summary
```dataviewjs

// 📆 날짜 설정 - 문서 제목에서 추출
let targetDate;

// 현재 문서의 제목에서 날짜 추출 시도 (yyyy-mm-dd 형식 찾기)
const titleDateMatch = dv.current().file.name.match(/(\d{4}-\d{2}-\d{2})/);

if (titleDateMatch) {
  // 제목에서 날짜를 찾은 경우
  targetDate = titleDateMatch[1];
} else if (dv.current().file.day) {
  // 제목에서 날짜를 찾지 못했지만 day 속성이 있는 경우
  targetDate = dv.current().file.day.toISODate();
} else {
  // 둘 다 없는 경우 오늘 날짜 사용
  const today = new Date();
  targetDate = today.toISOString().split('T')[0];
}

// 디버깅용 날짜 표시
dv.paragraph(`🗓️ **분석 날짜:** ${targetDate}`);

// 시간 포맷 함수 (기존 코드와 동일)
function formatDuration(totalMinutes) {
  if (totalMinutes === 0) return "0m";
  const minutes = Math.round(totalMinutes % 60);
  const hours = Math.floor(totalMinutes / 60);
  let result = "";
  if (hours > 0) result += `${hours}h`;
  if (minutes > 0) {
    if (hours > 0) result += " ";
    result += `${minutes}m`;
  }
  return result;
}

// 해당 날짜의 활동 데이터를 저장할 객체
const dailyActivities = {}; // { activityName: duration }
const activityEnergies = {}; // { activityName: { totalEnergy: value, totalDuration: value } }

// 해당 날짜의 파일 가져오기
const filePath = `01_Daily/Daily/${targetDate}.md`;
const page = dv.page(filePath);

if (!page) {
  dv.paragraph(`❌ **${targetDate}** 파일을 찾을 수 없습니다. 파일 경로를 확인하세요: \`${filePath}\``);
} else {
  // 파일 내용 가져오기
  const fileContent = await dv.io.load(page.file.path);

  // --- 섹션 추출 로직 (기존 코드와 동일) ---
  const plannerHeaderText = "Day planner";
  const headerRegex = new RegExp(`^#+\\s+${plannerHeaderText}\\s*$`, 'mi');
  const headerMatch = fileContent.match(headerRegex);
  let sectionContent = "";

  if (headerMatch) {
    const headerStartIndex = headerMatch.index;
    const matchedHeader = headerMatch[0];
    const contentAfterHeaderStart = fileContent.substring(headerStartIndex + matchedHeader.length);
    const nextHeaderMatch = contentAfterHeaderStart.match(/^\s*#{1,}\s+/m);
    const contentEndIndex = nextHeaderMatch ? nextHeaderMatch.index : contentAfterHeaderStart.length;
    sectionContent = contentAfterHeaderStart.substring(0, contentEndIndex).trim();
  }
  // --- 섹션 추출 로직 끝 ---

  if (sectionContent) {
    const lines = sectionContent.split('\n');
    const lineRegex = /^\s*[-*]\s+(?:\*\*)?(\d{1,2}:\d{2})(?:\*\*)?\s*[-–]\s*(?:\*\*)?(\d{1,2}:\d{2})(?:\*\*)?\s*(.*?)(?:\s+#.*)?$/;

    for (const line of lines) {
      if (!line.trim() || line.trim().startsWith('#')) continue;

      const match = line.trim().match(lineRegex);
      if (match) {
        let startTimeStr = match[1];
        let endTimeStr = match[2];
        let activityName = match[3].trim();

        if (startTimeStr.length === 4) startTimeStr = "0" + startTimeStr;
        if (endTimeStr.length === 4) endTimeStr = "0" + endTimeStr;

        // --- 활동 이름 정리 개선 (기존 코드와 유사하게 적용) ---
        const linkMatches = activityName.match(/!?\[\[(.*?)\]\]/g) || [];
        const tagMatches = line.trim().match(/#[\p{L}\p{N}_]+/gu) || []; // 라인 전체에서 태그 검색
        
        // console.log(`Line: "${line.trim()}", 찾은 태그:`, tagMatches); // 디버깅용

        let mainText = activityName;
        for (const link of linkMatches) {
          mainText = mainText.replace(link, '');
        }
        
        let tags = "";
        for (const tag of tagMatches) {
          mainText = mainText.replace(tag, ''); // 본문에서 태그 제거
          tags += tag + " ";
        }

        mainText = mainText.replace(/^\s*[\p{Emoji_Presentation}\p{Emoji}]\s*/u, '');
        mainText = mainText.replace(/^[ *_~]+|[ *_~]+$/g, '');
        mainText = mainText.replace(/[*_~]{1,3}(.*?)[*_~]{1,3}/g, '$1').trim();

        if (linkMatches.length > 0) {
          mainText = mainText + " " + linkMatches.join(" ");
        }

        activityName = (mainText || "(설명 없음)").trim();

        const energyTag = tagMatches.find(tag => tag.startsWith('#에너지'));
        let energyLevel = 0;
        if (energyTag) {
          const energyMatch = energyTag.match(/#에너지(\d+)/u);
          if (energyMatch && energyMatch[1]) {
            energyLevel = parseInt(energyMatch[1]);
            // console.log(`에너지 태그 발견: ${energyTag}, 추출된 레벨: ${energyLevel}`); // 디버깅용
          } else {
            // console.log(`에너지 값 추출 실패: ${energyTag}`); // 디버깅용
          }
        }
        // --- 활동 이름 정리 끝 ---

        try {
          const start = new Date(`${targetDate}T${startTimeStr}:00`);
          const end = new Date(`${targetDate}T${endTimeStr}:00`);

          if (isNaN(start.getTime()) || isNaN(end.getTime())) {
            console.warn(`유효하지 않은 시간 포맷 건너뜀: ${line.trim()}`);
            continue;
          }

          let adjustedEnd = end;
          if (start >= end) {
             if (startTimeStr.startsWith("2") && endTimeStr.startsWith("0")) {
               adjustedEnd = new Date(`${targetDate}T${endTimeStr}:00`);
               adjustedEnd.setDate(adjustedEnd.getDate() + 1);
             } else {
               console.warn(`종료 시간이 시작 시간보다 빠르거나 같은 항목 건너뜀: ${line.trim()}`);
               continue;
             }
          }

          const duration = (adjustedEnd - start) / (1000 * 60); // 분 단위

          if (duration > 0) {
            // 해당 날짜의 활동 정보 저장
            dailyActivities[activityName] = (dailyActivities[activityName] || 0) + duration;

            // 에너지 데이터 저장
            if (energyLevel > 0) {
              if (!activityEnergies[activityName]) {
                activityEnergies[activityName] = { totalEnergy: 0, totalDuration: 0 };
              }
              // 에너지 * 시간 가중치 누적
              activityEnergies[activityName].totalEnergy += energyLevel * duration;
              activityEnergies[activityName].totalDuration += duration;
            }
          }
        } catch (e) {
          console.error(`시간 처리 오류: ${line.trim()}`, e);
          dv.span(`❌ 시간 처리 오류 발생: ${line.trim()}. 개발자 콘솔(Ctrl+Shift+I) 확인`);
        }
      } else if (line.trim()) {
        // 매치되지 않은 라인 로깅 (디버깅용)
        // console.log(`매치되지 않은 라인: "${line.trim()}"`);
      }
    }
  } else {
    dv.paragraph(`ℹ️ ${targetDate} 파일에서 '${plannerHeaderText}' 섹션을 찾을 수 없거나 내용이 없습니다.`);
  }

  // --- 테이블 생성 로직 ---
  const activityNames = Object.keys(dailyActivities);

  if (activityNames.length === 0) {
    dv.paragraph("📊 해당 날짜에 분석할 활동 데이터가 없습니다.");
  } else {
    // 테이블 헤더 생성
    const headers = ["활동", "시간", "비율", "평균 에너지"];

    // 테이블 데이터 생성
    const tableData = [];
    let totalDayMinutes = 0;

    // 해당 날짜의 총 시간 계산
    for (const activity of activityNames) {
      totalDayMinutes += dailyActivities[activity];
    }

    // 각 활동별 데이터 생성
    for (const activity of activityNames) {
      const duration = dailyActivities[activity];
      const formattedDuration = formatDuration(duration);

      // 퍼센트 계산
      const percentage = totalDayMinutes > 0 ? Math.round((duration / totalDayMinutes) * 100) : 0;

      // 평균 에너지 계산
      let avgEnergy = "-";
      if (activityEnergies[activity] && activityEnergies[activity].totalDuration > 0) {
        // 해당 활동의 총 에너지 가중 합계를 총 시간으로 나누어 평균 계산
        const energyAvg = Math.round(activityEnergies[activity].totalEnergy / activityEnergies[activity].totalDuration);
        avgEnergy = `${energyAvg}`;
      }

      tableData.push([
        activity,
        formattedDuration,
        `${percentage}%`,
        avgEnergy
      ]);
    }

    // 테이블 데이터 정렬 (시간 기준 내림차순)
    tableData.sort((a, b) => {
      const getMinutes = (timeStr) => {
        if (timeStr === "-" || timeStr === "0m") return 0;
        let totalMinutes = 0;
        const hourMatch = timeStr.match(/(\d+)h/);
        const minuteMatch = timeStr.match(/(\d+)m/);
        if (hourMatch) totalMinutes += parseInt(hourMatch[1]) * 60;
        if (minuteMatch) totalMinutes += parseInt(minuteMatch[1]);
        return totalMinutes;
      };
      // 시간은 두 번째 열(index 1)
      return getMinutes(b[1]) - getMinutes(a[1]);
    });

    // 테이블 Markdown 생성
    let table = `| ${headers.join(" | ")} |\n`;
    table += `| ${headers.map(_ => "---").join(" | ")} |\n`;
    for (const row of tableData) {
      table += `| ${row.join(" | ")} |\n`;
    }

    // 테이블 출력
    dv.paragraph(table);

    // --- 요약 정보 계산 및 출력 ---
    let totalEnergyWeighted = 0;
    let totalEnergyDuration = 0;

    // 해당 날짜의 전체 평균 에너지 계산
    for (const activity in activityEnergies) {
        totalEnergyWeighted += activityEnergies[activity].totalEnergy;
        totalEnergyDuration += activityEnergies[activity].totalDuration;
    }

    const overallAvgEnergy = totalEnergyDuration > 0 ?
      Math.round(totalEnergyWeighted / totalEnergyDuration) : 0;

    dv.paragraph(`**총 기록 시간:** ${formatDuration(totalDayMinutes)} | **평균 에너지:** ${overallAvgEnergy}`);
  }
}
```