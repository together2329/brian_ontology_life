## Day planner

> 에너지 색상 기준  
> 🟢 100 | 🟩 90–99 | 🟦 80–89 | 🟨 70–79 | 🟧 60–69 | 🟥 50–59 | ⬛ 0–49

- 07:20-08:00 📝 **반성 & 리뷰**  #에너지80 🟦
  - NotebookLM으로 반성 작성  
    - 하루 리뷰 및 상세 피드백
  - 클로드 + Chat GPT
    - 에너지 레벨 시각화

- 08:00-08:30 [[산책]] #에너지90 🟩 
  - 해이, 지구와 함께 산책  
  - 화창한 날씨에 감사

- 08:30-09:00 🛁 **준비 & 아침** #에너지90 🟩 
  - 세면 및 외출 준비  
  - 견과류 + 계란 + 사과  
  - 커피 한 잔 내리기

- **09:00-09:50** 🎧 **강의 청취 (출근)** #에너지80 🟦  
  - Chat GPT와 영어 공부 10분 ( 매일 하면 좋을 듯 ! ) -> Note Page 로 만들어서 연결
  - 내가 만든 영상 2개 보기 ( 퀄리티 좋음! ) -> 확실히.. 생산적인 활동에 매력을 느낀다

- **09:50 - 11:50** 💼 **오전 업무** #에너지60 🟧 
  - email 정독
  - bid 수정
  - doe simulation 결과 확인

- **11:50-12:20** [[식사]] #에너지80 🟦
  - 건강한 식단 (먹느라 약간 정신없음)

- **12:20 - 13:00**  [[산책]]  #에너지90 🟩
  - 김TL님과 좋은 날씨에 이런 저런 이야기 ! 

* **13:00 - 13:10** [[스트레칭]] #에너지100 🟢
	* 이거 30분 단위로 계속 하면 삶의 질 크게 향상 시킬 듯 !!!
	* 짧은 스트레칭 ( 눈감고 목 뒤로 하기 )

- **13:10 - 13:30** 📡 ![[MSG & Shared SRAM#^RATELMSG2]] #에너지70 🟨
	- AXI User 쪽이 하나도 안 되어 있음... 이건 시간이 오래 걸리는 거라 보류
​
* **13:30 - 15:30** ![[GZR_Task#^GZRTASK20250420-185931]] #에너지100 🟢
	* Ratel2, GZR 모두 AWUSER에 연결 완료 -> TC 요청 필요

- **15:30 - 15:45** [[스트레칭]] #에너지90 🟩 
	- 스트레칭 자주 자주 하자 !!

- **15:45 - 16:15** ![[GZR_Task#^GZRTASK20250420-185840]] #에너지90 🟩 
	- dev num을 ari enable 0 일때 0으로 들어가도록 변경

- **16:15 - 17:20** ![[GZR_Task#^GZRTASK20250420-184732]] #에너지90 🟩 
	- source interrupt 보도록 변경
	- slot 2개로 추가



```dataviewjs

// 📆 날짜 설정 - 문서 제목에서 추출
let targetDate;

// 현재 문서의 제목에서 날짜 추출 시도 (yyyy-mm-dd 형식 찾기)
const titleDateMatch = dv.current().file.name.match(/(\d{4}-\d{2}-\d{2})/);

if (titleDateMatch) {
  // 제목에서 날짜를 찾은 경우
  targetDate = titleDateMatch[1];
} else if (dv.current().file.day) {
  // 제목에서 날짜를 찾지 못했지만 day 속성이 있는 경우
  targetDate = dv.current().file.day.toISODate();
} else {
  // 둘 다 없는 경우 오늘 날짜 사용
  const today = new Date();
  targetDate = today.toISOString().split('T')[0];
}

// 디버깅용 날짜 표시
dv.paragraph(`🗓️ **분석 날짜:** ${targetDate}`);

// 시간 포맷 함수 (기존 코드와 동일)
function formatDuration(totalMinutes) {
  if (totalMinutes === 0) return "0m";
  const minutes = Math.round(totalMinutes % 60);
  const hours = Math.floor(totalMinutes / 60);
  let result = "";
  if (hours > 0) result += `${hours}h`;
  if (minutes > 0) {
    if (hours > 0) result += " ";
    result += `${minutes}m`;
  }
  return result;
}

// 해당 날짜의 활동 데이터를 저장할 객체
const dailyActivities = {}; // { activityName: duration }
const activityEnergies = {}; // { activityName: { totalEnergy: value, totalDuration: value } }

// 해당 날짜의 파일 가져오기
const filePath = `01_Daily/Daily/${targetDate}.md`;
const page = dv.page(filePath);

if (!page) {
  dv.paragraph(`❌ **${targetDate}** 파일을 찾을 수 없습니다. 파일 경로를 확인하세요: \`${filePath}\``);
} else {
  // 파일 내용 가져오기
  const fileContent = await dv.io.load(page.file.path);

  // --- 섹션 추출 로직 (기존 코드와 동일) ---
  const plannerHeaderText = "Day planner";
  const headerRegex = new RegExp(`^#+\\s+${plannerHeaderText}\\s*$`, 'mi');
  const headerMatch = fileContent.match(headerRegex);
  let sectionContent = "";

  if (headerMatch) {
    const headerStartIndex = headerMatch.index;
    const matchedHeader = headerMatch[0];
    const contentAfterHeaderStart = fileContent.substring(headerStartIndex + matchedHeader.length);
    const nextHeaderMatch = contentAfterHeaderStart.match(/^\s*#{1,}\s+/m);
    const contentEndIndex = nextHeaderMatch ? nextHeaderMatch.index : contentAfterHeaderStart.length;
    sectionContent = contentAfterHeaderStart.substring(0, contentEndIndex).trim();
  }
  // --- 섹션 추출 로직 끝 ---

  if (sectionContent) {
    const lines = sectionContent.split('\n');
    const lineRegex = /^\s*[-*]\s+(?:\*\*)?(\d{1,2}:\d{2})(?:\*\*)?\s*[-–]\s*(?:\*\*)?(\d{1,2}:\d{2})(?:\*\*)?\s*(.*?)(?:\s+#.*)?$/;

    for (const line of lines) {
      if (!line.trim() || line.trim().startsWith('#')) continue;

      const match = line.trim().match(lineRegex);
      if (match) {
        let startTimeStr = match[1];
        let endTimeStr = match[2];
        let activityName = match[3].trim();

        if (startTimeStr.length === 4) startTimeStr = "0" + startTimeStr;
        if (endTimeStr.length === 4) endTimeStr = "0" + endTimeStr;

        // --- 활동 이름 정리 개선 (기존 코드와 유사하게 적용) ---
        const linkMatches = activityName.match(/!?\[\[(.*?)\]\]/g) || [];
        const tagMatches = line.trim().match(/#[\p{L}\p{N}_]+/gu) || []; // 라인 전체에서 태그 검색
        
        // console.log(`Line: "${line.trim()}", 찾은 태그:`, tagMatches); // 디버깅용

        let mainText = activityName;
        for (const link of linkMatches) {
          mainText = mainText.replace(link, '');
        }
        
        let tags = "";
        for (const tag of tagMatches) {
          mainText = mainText.replace(tag, ''); // 본문에서 태그 제거
          tags += tag + " ";
        }

        mainText = mainText.replace(/^\s*[\p{Emoji_Presentation}\p{Emoji}]\s*/u, '');
        mainText = mainText.replace(/^[ *_~]+|[ *_~]+$/g, '');
        mainText = mainText.replace(/[*_~]{1,3}(.*?)[*_~]{1,3}/g, '$1').trim();

        if (linkMatches.length > 0) {
          mainText = mainText + " " + linkMatches.join(" ");
        }

        activityName = (mainText || "(설명 없음)").trim();

        const energyTag = tagMatches.find(tag => tag.startsWith('#에너지'));
        let energyLevel = 0;
        if (energyTag) {
          const energyMatch = energyTag.match(/#에너지(\d+)/u);
          if (energyMatch && energyMatch[1]) {
            energyLevel = parseInt(energyMatch[1]);
            // console.log(`에너지 태그 발견: ${energyTag}, 추출된 레벨: ${energyLevel}`); // 디버깅용
          } else {
            // console.log(`에너지 값 추출 실패: ${energyTag}`); // 디버깅용
          }
        }
        // --- 활동 이름 정리 끝 ---

        try {
          const start = new Date(`${targetDate}T${startTimeStr}:00`);
          const end = new Date(`${targetDate}T${endTimeStr}:00`);

          if (isNaN(start.getTime()) || isNaN(end.getTime())) {
            console.warn(`유효하지 않은 시간 포맷 건너뜀: ${line.trim()}`);
            continue;
          }

          let adjustedEnd = end;
          if (start >= end) {
             if (startTimeStr.startsWith("2") && endTimeStr.startsWith("0")) {
               adjustedEnd = new Date(`${targetDate}T${endTimeStr}:00`);
               adjustedEnd.setDate(adjustedEnd.getDate() + 1);
             } else {
               console.warn(`종료 시간이 시작 시간보다 빠르거나 같은 항목 건너뜀: ${line.trim()}`);
               continue;
             }
          }

          const duration = (adjustedEnd - start) / (1000 * 60); // 분 단위

          if (duration > 0) {
            // 해당 날짜의 활동 정보 저장
            dailyActivities[activityName] = (dailyActivities[activityName] || 0) + duration;

            // 에너지 데이터 저장
            if (energyLevel > 0) {
              if (!activityEnergies[activityName]) {
                activityEnergies[activityName] = { totalEnergy: 0, totalDuration: 0 };
              }
              // 에너지 * 시간 가중치 누적
              activityEnergies[activityName].totalEnergy += energyLevel * duration;
              activityEnergies[activityName].totalDuration += duration;
            }
          }
        } catch (e) {
          console.error(`시간 처리 오류: ${line.trim()}`, e);
          dv.span(`❌ 시간 처리 오류 발생: ${line.trim()}. 개발자 콘솔(Ctrl+Shift+I) 확인`);
        }
      } else if (line.trim()) {
        // 매치되지 않은 라인 로깅 (디버깅용)
        // console.log(`매치되지 않은 라인: "${line.trim()}"`);
      }
    }
  } else {
    dv.paragraph(`ℹ️ ${targetDate} 파일에서 '${plannerHeaderText}' 섹션을 찾을 수 없거나 내용이 없습니다.`);
  }

  // --- 테이블 생성 로직 ---
  const activityNames = Object.keys(dailyActivities);

  if (activityNames.length === 0) {
    dv.paragraph("📊 해당 날짜에 분석할 활동 데이터가 없습니다.");
  } else {
    // 테이블 헤더 생성
    const headers = ["활동", "시간", "비율", "평균 에너지"];

    // 테이블 데이터 생성
    const tableData = [];
    let totalDayMinutes = 0;

    // 해당 날짜의 총 시간 계산
    for (const activity of activityNames) {
      totalDayMinutes += dailyActivities[activity];
    }

    // 각 활동별 데이터 생성
    for (const activity of activityNames) {
      const duration = dailyActivities[activity];
      const formattedDuration = formatDuration(duration);

      // 퍼센트 계산
      const percentage = totalDayMinutes > 0 ? Math.round((duration / totalDayMinutes) * 100) : 0;

      // 평균 에너지 계산
      let avgEnergy = "-";
      if (activityEnergies[activity] && activityEnergies[activity].totalDuration > 0) {
        // 해당 활동의 총 에너지 가중 합계를 총 시간으로 나누어 평균 계산
        const energyAvg = Math.round(activityEnergies[activity].totalEnergy / activityEnergies[activity].totalDuration);
        avgEnergy = `${energyAvg}`;
      }

      tableData.push([
        activity,
        formattedDuration,
        `${percentage}%`,
        avgEnergy
      ]);
    }

    // 테이블 데이터 정렬 (시간 기준 내림차순)
    tableData.sort((a, b) => {
      const getMinutes = (timeStr) => {
        if (timeStr === "-" || timeStr === "0m") return 0;
        let totalMinutes = 0;
        const hourMatch = timeStr.match(/(\d+)h/);
        const minuteMatch = timeStr.match(/(\d+)m/);
        if (hourMatch) totalMinutes += parseInt(hourMatch[1]) * 60;
        if (minuteMatch) totalMinutes += parseInt(minuteMatch[1]);
        return totalMinutes;
      };
      // 시간은 두 번째 열(index 1)
      return getMinutes(b[1]) - getMinutes(a[1]);
    });

    // 테이블 Markdown 생성
    let table = `| ${headers.join(" | ")} |\n`;
    table += `| ${headers.map(_ => "---").join(" | ")} |\n`;
    for (const row of tableData) {
      table += `| ${row.join(" | ")} |\n`;
    }

    // 테이블 출력
    dv.paragraph(table);

    // --- 요약 정보 계산 및 출력 ---
    let totalEnergyWeighted = 0;
    let totalEnergyDuration = 0;

    // 해당 날짜의 전체 평균 에너지 계산
    for (const activity in activityEnergies) {
        totalEnergyWeighted += activityEnergies[activity].totalEnergy;
        totalEnergyDuration += activityEnergies[activity].totalDuration;
    }

    const overallAvgEnergy = totalEnergyDuration > 0 ?
      Math.round(totalEnergyWeighted / totalEnergyDuration) : 0;

    dv.paragraph(`**총 기록 시간:** ${formatDuration(totalDayMinutes)} | **평균 에너지:** ${overallAvgEnergy}`);
  }
}
```

### 리뷰


제공해주신 하루 일정을 살펴보니, **다양한 활동들을 통해 에너지와 만족감을 얻으시는 모습**이 여러 출처에서 언급된 내용들과 연결되어 흥미롭습니다.

- **07:20-08:00 📝 반성 & 리뷰 #에너지80 🟦**: 하루를 시작하며 **반성과 리뷰를 통해 마음의 차분함을 찾는 것**은 "아침에 일어나 30분 넘게 멍때리다가, 이 행복꾸러미를 읽고 쓰면서 마음의 차분함을 찾았습니다."라는 내용과 관련될 수 있습니다. 또한, **자신의 글을 읽으며 힘을 얻는 경험**은 "내가 약간 다운 될 때 내 위키의 글들을 보고 있으면 힘을 얻는다."라는 구절과 연결될 수 있으며, 이는 '평생 읽고 싶은 책의 Book 파트'를 읽는 행위가 마음의 평안과 집중력을 높여준다는 내용과 함께 언급되어 있습니다.
- **08:00-08:30 🚶 산책 #에너지90 🟩**: **산책을 통해 활력을 얻고 화창한 날씨에 감사하는 마음**은 "가고 안 가고에 따라서 하루를 시작하는 느낌이 확연히 다르다. 다녀오면 굉장히 평온하고 활력이 생기는 하루가 된다." 또는 "아주 간만에 산책을 나갔는데 기분이 너무나 좋았습니다. 삶은. 매일 매일은. 선물입니다."와 같이 **산책이 긍정적인 감정 변화를 가져다주는 경험**을 설명하는 내용과 일치합니다. 특히, 사랑하는 사람(해이, 지구)과 함께하는 산책을 좋아한다는 점은 "사랑하는 아름이와 함께하는 산책을 좋아한다."라는 내용과 유사합니다.
- **08:30-09:00 🛁 준비 & 아침 #에너지90 🟩**: 특별히 관련된 내용은 보이지 않습니다.
- **09:00-09:50 🎧 강의 청취 (출근) #에너지80 🟦**: **새로운 것을 배우는 것을 좋아하고 성장하는 것을 즐기는 마음**은 "영어 좋아하고, 발표 좋아하고 새로운거 배우는거 좋아하고 ( 성장하는거 좋아하고 ) 이 모든게 충족 되는 환경에 있다."라는 내용과 연결될 수 있습니다. 또한, **자신이 만든 영상을 보고 퀄리티에 만족하며 생산적인 활동에 매력을 느끼는 것**은 "모든 것을 고려해서 설계해보는 기쁨. 그리고 Front end와 Back end적인 지식을 배울 수 있는 기회와 SoC팀 장창은 수석님 김초롱 책임님으로 부터의 값진 경험을 배울 수 있는 기회."와 같이 **직접 설계하고 개발할 때 느끼는 즐거움** 또는 **공부한 것을 잘 이해해서 세미나 진행할 때 (알려줄 때) 느끼는 즐거움**과 유사한 맥락으로 이해될 수 있습니다.
- **09:50 - 11:50 💼 오전 업무 #에너지60 🟧**: 구체적인 업무 내용과 관련된 직접적인 언급은 없지만, **일에 온 에너지를 집중하는 것의 중요성**은 "현재 하고 있는 일에 온 에너지를 쏟기. 온 에너지를 쏟을 수 있는 환경을 만들기."에서 강조되고 있습니다.
- **11:50-12:20 🍽️ 점심 식사 #에너지80 🟦**: **건강한 식단을 통해 몸과 마음의 편안함**을 느끼는 것은 "항상 소식을 하면 몸과 마음이 편안했는데, 많이 먹으면 몸이 무겁고 마음도 편하지 않았다."라는 내용과 연결될 수 있습니다.
- **12:20 - 13:00 🚶‍♂️ 산책 #에너지90 🟩**: 김TL님과 함께 **좋은 날씨에 이야기를 나누며 산책하는 것**은 "성현TL님과 점심 먹고 산책을 다녀왔습니다. 점심 시간에 운동도 하고 이런 저런 이야기 나누며 걸을 수 있어 좋습니다. 이런 소중한 시간에 감사합니다."라는 내용과 같이 **소중한 사람들과 함께하는 시간의 가치**를 보여줍니다.
- **13:00 - 13:10 짧은 스트레칭 ( 눈감고 목 뒤로 하기 ) #에너지100 🟢**: 스트레칭 자체에 대한 직접적인 언급은 없지만, **규칙적인 실천을 통해 삶의 질을 향상시킬 수 있다는 기대감**은 "느낀점은 조금이라도 매일 매일 하나가면 습관이 되어 추진력이 생긴다는 것입니다. 무엇이든 그런 추진력이 필요합니다."라는 내용과 연결될 수 있습니다.
- **13:10 - 13:30 📡 Ratel Msg TX 심층 확인 #에너지70 🟨**: **어려운 문제를 해결하는 과정**은 "어려운 문제를 해결 할 때" 즐거움을 느낀다는 내용과 관련될 수 있습니다. 비록 당장 해결되지 않았지만, 문제 해결을 위한 시도 자체가 의미 있을 수 있습니다.
- **13:30 - 15:30 Requester , Destination Segment 추가 #에너지100 🟢**: **직접 설계하고 개발하는 과정** 과 **목표로 하는 것에 대해 바로 행동했을 때 느끼는 만족감** "특히 목표로 하는 것에 대한 실행이라 더 그렇습니다. 위의 문구를 가슴 속에 새기고, 더 실행하겠습니다 !!! 두려워말고, 우선 움직이겠습니다 !!!!"과 연결될 수 있습니다. 또한, **성과가 나왔을 때 자율적인 환경에서 일하는 것에 대한 기쁨과 만족** 과도 관련이 있어 보입니다.
- **15:30 - 15:45 스트레칭 !! #에너지90 🟩**: **스트레칭을 자주 하는 것의 중요성**은 앞서 언급된 규칙적인 습관 형성에 대한 내용 과 연결될 수 있습니다.
- **15:45 - 16:15 DUT Return Error #에너지90 🟩** 및 **16:15 - 17:20 Speed Interrupt 추가 #에너지90 🟩**: 이러한 **기술적인 문제 해결 과정** 역시 "어려운 문제를 해결 할 때" 느끼는 즐거움과 관련될 수 있으며, **IP 공부, Spec 공부** 를 통해 얻은 지식을 활용하는 경험일 수 있습니다.

전반적으로 볼 때, **업무적인 성취, 건강 관리, 인간 관계, 그리고 새로운 것을 배우고 성장하는 경험**들이 하루의 에너지 수준과 만족도에 긍정적인 영향을 미치는 것을 알 수 있습니다. 특히, **행동하고 실행하는 것**, **소중한 사람들과 교류하는 것**, 그리고 **자신에게 의미 있는 활동에 집중하는 것**이 높은 에너지 레벨과 연결되는 것으로 보입니다.

### AI 




> [!note] AI 활동 분석
> 오늘 날짜는 2025-04-17입니다. 다음은 오늘 수행한 주요 활동, 시간 배분 패턴, 에너지 흐름 등의 요약입니다.
> 
> 주요 활동:
> 1. 업무: 2h (평균 에너지: 60)
> 2. GZR_Task#^GZRTASK20250420-185931: 2h (평균 에너지: 100)
> 3. 산책: 1h 10m (평균 에너지: 90)
> 4. GZR_Task#^GZRTASK20250420-184732: 1h 5m (평균 에너지: 90)
> 5. 강의 청취: 1h 5m (평균 에너지: 80)
> 6. 반성 & 리뷰: 40m (평균 에너지: 80)
> 7. 준비 & 아침: 30m (평균 에너지: 90)
> 8. 식사: 30m (평균 에너지: 80)
> 9. GZR_Task#^GZRTASK20250420-185840: 30m (평균 에너지: 90)
> 10. 스트레칭: 25m (평균 에너지: 94)
> 11. MSG & Shared SRAM#^RATELMSG2: 20m (평균 에너지: 70)
> 
> 시간 배분:
> - 업무와 개인적인 활동은 전반적으로 균등하게 분배되어 있습니다. 업무와 개인적인 활동 시간의 비율은 4:6입니다.
> - 오전 10:00 ~ 14:00 사이에 대부분의 업무와 개인적인 활동을 수행하였고, 오후 14:00 ~ 18:00 사이에는 스트레칭과 메시지 교환을 원활히 수행했습니다.
> 
> 에너지 흐름:
> - 평균 에너지 수치는 84로 높은 에너지 수준을 보이고 있습니다. 하지만, 업무를 수행하는 동안 에너지 수치가 낮게 유지되고 있는 것을 확인할 수 있습니다.
> - 산책, 스트레칭, 강의 청취, 그리고 GZR_Task#^GZRTASK20250420-184732에서는 에너지 수치가 90 이상으로 높게 유지되고 있는 것을 확인할 수 있습니다.
> 
> 지속적인 에너지 관리와 잠재력 개발을 위한 방향성 요령으로, 업무 시간에는 잠재력 개발을 위한 훈련을 통해 에너지 수치를 높이고, 기타 활동에서는 이동, 운동, 강의 청취, 그리고 개인적인 활동에 집중하여 에너지 수치를 높이는 것이 중요할 것입니다.
