# Raw Data

Place raw local source files here.

This directory is ignored by git by default because it may contain private
personal records, source documents, screenshots, exports, images, or logs.

When importing raw data:

- keep the original file unchanged
- record source path, import date, and hash when possible
- write normalized outputs under `life/imports/`
- do not publish raw records unless explicitly intended

