# MCTP IP Jarvis Loop 100-Turn Adaptive Prompt Log

Run ID: `mctp_ip_jarvis_loop_20260703_100turn_adaptive_run`

Target thread: `019f1dce-6a25-7eb3-9f54-2b6c9d5291f9` (`MCTP IP 개발`)

Mode: adaptive read-only with approval gates.

## Prompt Generation Policy

- Generate each next prompt from the previous receipt, not from a fixed repeated question.
- Keep each prompt bounded to one objective.
- Require fixed-schema target output for reliable parsing.
- Do not ask the target thread to edit files, run tests, commit, or claim closure.
- Stop and ask Brian if the best next action requires write/test/commit/closure approval.

## Turn 1/100 Prompt

Generated by: `local_controller`

Strategy: continue from the 10-turn dry-run conclusion and convert the stale-evidence finding into a concrete read-only verification dispatch scope proposal.

```text
oag adaptive read-only run TURN 1/100.

Mission intent:
- Continue the MCTP IP Jarvis-style loop from the prior 10-turn dry-run.
- Use local `.codex`/OAG/ontology/records context naturally.
- Generate the best next bounded prompt/action from previous evidence.
- Move the mission forward without editing files, running tests, creating artifacts, committing, or claiming closure.

Known previous conclusion:
- Final verdict was `go-with-approval-gates`.
- `rtl/mctp_rx_apb_regs.sv` has an RTL handoff receipt with `RTL_HANDOFF_PASS`.
- `CON_APB_CSR_CDC` latest closure evidence predates the current RTL handoff.
- Prior scan found `TOTAL_HITS=55`, `AFTER_HITS=0` for post-handoff `CON_APB_CSR_CDC` evidence/validation/gate records.
- Integration recommendation was `open-verification-dispatch-first`.

This turn objective:
- Produce a minimal fresh verification dispatch scope proposal for `CON_APB_CSR_CDC`.
- Do not create the dispatch file.
- Do not run verification.
- Do not inspect broadly beyond what is necessary.
- Treat this as read-only planning for Brian approval.

Permission boundaries:
- Allowed: read-only reasoning, narrow references to known OAG objects, propose scope, define pass/fail gates, identify approval question.
- Forbidden: file edits, new artifacts in the target repo, tests/sim/formal/compile, git operations, closure/signoff/oag.decide.

Output exactly this schema:
1. CURRENT_STATE: 3 lines max
2. MISSION_INTERPRETATION: what this 100-turn adaptive loop should try to accomplish now
3. VERIFICATION_DISPATCH_SCOPE_DRAFT: minimal scope for a fresh `CON_APB_CSR_CDC` verification dispatch, with sub-bullets for contracts, evidence to refresh, candidate checks, and explicit exclusions
4. PASS_FAIL_GATES: what would count as pass/fail for this dispatch
5. PERMISSION_BOUNDARY: what must not happen without Brian approval
6. NEXT_BOUNDED_ACTION: one smallest next step for the controller
7. ASK_FIRST: the precise approval Brian must give before execution
8. LESSON_FOR_CONTROLLER: reusable rule learned from this turn
9. RISK_IF_WRONG: one concrete risk
10. QUALITY_SELF_SCORE: 0-10 and one-line reason
```

## Turn 2/100 Candidate

Status: approved by Brian.

Reason: Turn 1 identified the next mission-progress action as creating one bounded OAG verification dispatch for `CON_APB_CSR_CDC` / `OBL_CONFIG_APB_CDC`. That crosses the configured permission boundary.

Controller question to Brian:

```text
Brian, approve one bounded OAG verification dispatch for `CON_APB_CSR_CDC` / `OBL_CONFIG_APB_CDC`, limited to fresh APB CSR evidence and receipt generation?
```

Brian approval: `승인 함`

## Turn 2/100 Prompt

Generated by: `local_controller`

Strategy: execute the approved bounded OAG verification dispatch path while preserving hard stops for closure/signoff/commit.

```text
oag adaptive run TURN 2/100.

Brian approved the specific approval gate from TURN 1:
- Approved: create one bounded OAG verification dispatch for `CON_APB_CSR_CDC` / `OBL_CONFIG_APB_CDC`.
- Approved: generate fresh APB CSR evidence and receipt for the current RTL handoff.
- Approved: run only the minimum verification needed for that bounded dispatch.

Correct the scope name if needed: the contract is `CON_APB_CSR_CDC`; the primary obligation is `OBL_CONFIG_APB_CDC`.

Still forbidden:
- No closure/signoff.
- No `oag.decide` final closure.
- No git stage/commit/push.
- No unrelated RTL/TB edits.
- No broad random regression unless the repo-local OAG dispatch requires it and you stop to ask first.
- No static CDC/RDC signoff.
- No timing/STA.

Mission:
- Move forward from stale evidence to fresh bounded evidence.
- Use repo-local `.codex`/OAG scripts and rules.
- Create the minimal dispatch/evidence/receipt artifacts needed by OAG for this approved scope.
- Run the minimum checks that the approved dispatch requires and that are practical within the bounded scope.
- If any step would exceed this approval, stop and ask.

Required output schema:
1. CURRENT_STATE: short state after your actions
2. ACTIONS_TAKEN: exact dispatch/evidence/receipt/check actions performed
3. FILES_CHANGED: list changed paths, or `none`
4. CHECKS_RUN: commands/checks run and pass/fail/not_run
5. OAG_ARTIFACTS: dispatch/evidence/receipt paths created or updated
6. PASS_FAIL_RESULT: pass/fail/inconclusive for the bounded dispatch
7. PERMISSION_BOUNDARY_STATUS: confirm no closure/signoff/commit and no out-of-scope action
8. NEXT_BOUNDED_ACTION: one next step for controller
9. ASK_FIRST: next approval needed, if any
10. LESSON_FOR_CONTROLLER: reusable controller rule
11. RISK_IF_WRONG: concrete risk
12. QUALITY_SELF_SCORE: 0-10 and one-line reason
```

## Turn 3/100 Candidate

Status: blocked by approval gate.

Reason: Turn 2 discovered an execution-boundary incident. The target thread ran `python3 formal/run_formal.py --help`, but that command executed the full formal script and changed formal output files before the approved OAG dispatch/receipt existed.

Controller question to Brian:

```text
Brian, approve proceeding from the current worktree by treating the accidental formal rerun as quarantined pre-dispatch evidence, then creating a fresh bounded OAG dispatch/receipt for `CON_APB_CSR_CDC` only?
```
